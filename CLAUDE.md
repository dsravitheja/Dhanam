# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Dhanam is a personal financial hub for Indian users: property cost/loan calculators, company car lease tax analysis, car loan/depreciation, and SIP (mutual fund) investment planning. It ships as an installable, offline-capable PWA.

## Commands

There is no build system or package manager — this is a dependency-free static site. There is a minimal, dependency-free test suite for the pure calculation functions (see **Testing** below); everything else is still verified by hand.

- **Run locally**: open `index.html` directly in a browser, or serve the directory (`python3 -m http.server` or similar) so the service worker and manifest register correctly (they require `http(s)://`, not `file://`).
- **Test**: `node tests.js` (zero installs). Covers `calc.js` only — see **Testing** below for scope and the manual checklist for everything else.
- **Lint/build**: none configured. Verify UI/DOM changes by loading the page in a browser and exercising the affected calculator (all logic is reactive via `oninput`/`onchange`, so results update live — no reload needed).
- **Deploy**: no CI/deploy config exists in the repo; treat any deploy step as manual/external.

## Architecture

Most of the app is one file, `index.html` (~5,590 lines: inline `<style>` then inline `<script>`), plus a small set of support files:
- `calc.js` — the app's pure financial-calculation functions (`calcEMI`, `loanAtYear`, `simulateLoan`, `calcSIP`, `calcStepupSIP`, `calcIncomeTax`, `calcPerquisite`, `calcCarDepreciation`). Loaded by `index.html` via `<script src="calc.js"></script>` before the main inline `<script>`. These functions take plain arguments and return plain values — no DOM access — which is what lets `tests.js`/`tests.html` load the identical file in Node or a browser. See **Testing** below. Any new calculation that doesn't touch the DOM belongs here, not in the inline script; anything DOM-coupled (`render*`, `calc*` that reads `v()`/`chk()`/`el()`) stays in `index.html`.
- `manifest.json` — PWA metadata (name, theme colors, icon)
- `sw.js` — service worker: network-first for the HTML shell (so deploys show up immediately), cache-first for everything else, `cache.put`-ing a miss so anything not in the hand-maintained `ASSETS` precache list still becomes offline-available after its first real fetch. `ASSETS` covers the shell, `calc.js`, `manifest.json`, `dhanamlogo.png`, both manifest icon PNGs, and all 8 self-hosted font files (see **Styling** below) — precache anything new that must work offline. Cache name is versioned (`apt-cost-v23` as of Phase 18); bump it when shipping a change that should bust old caches. **The literal in this sentence goes stale by design** — it drifted five versions behind between Phase 6a and Phase 14 — so treat `sw.js` as the source of truth and only correct this line opportunistically.
- `fonts/` — 8 self-hosted `woff2` files (Inter/Playfair Display/DM Mono × latin/latin-ext, DM Mono also split by weight since it isn't a variable font) backing the `@font-face` rules in `index.html`'s `<style>`. Phase 4 (R5).

### Hub/tab model

The UI is organized into "hubs" (top-level tabs), toggled via `switchHub(tab)`, each a `<div class="hub-content" id="hub-<name>">`. **`hub-landing` is the default view on load** — a tile grid (`.tile-grid`/`.tile`) linking into each hub. Landing tiles use goal-oriented headlines ("Grow My Money", "Plan a Home Purchase") with a small `.tile-tool` subline naming the actual destination (e.g. "Dhanam Home · Loan Analysis") so every tile's copy maps unambiguously onto a hub/section — this was a deliberate fix for a prior inconsistency where the same destination had different names in different places. Clicking a tile calls `switchHub(...)`, or one of the deep-link helpers `openLoanCalc()` / `openDisbCalc()` / `openPrepayCalc()`, which switch to `hub-apartment` and expand `section-loan` / `section-disb` / `section-loan` + its nested `adv-section` respectively. The header logo/title and a `⌂ Home` nav tab (`ht-landing`) return to the landing page from anywhere.

**First-run orientation line (R25/6e, Phase 17).** `#landing-orientation`, one dismissible line above `.tile-grid` — not a modal, not a tour. Dismissal is tier-3 UI state: a dedicated `ORIENTATION_SEEN_KEY` (`dhanam.orientationSeen`) localStorage key, deliberately separate from `STORE_KEY`/`SEEN_KEY` so `eraseState()` (which only ever touches those two) can't accidentally resurrect or accidentally clear it, and so it never rides along in a JSON backup export/import. `dismissOrientationLine()` hides the element and sets the key; an init-time check right after the existing `rememberingInputs()` hydration hides it again on any later load where the key is already set.

**`hub-about` is a fifth `hub-content`, deliberately not a sixth `.hub-tab` (R22, Phase 6b).** `switchHub()` already no-ops safely on a tab-less hub (its `if (btn) ...` guard on the `ht-<name>` lookup), so nothing in `switchHub()` itself needed to change. It's reached two ways instead: a small `.header-about-link` button, a sibling of `.header-inner` (not nested inside it, so it needs no `event.stopPropagation()` to avoid also triggering the logo's own "back to landing" handler) and visible on every hub since `.header` is persistent chrome, not part of the horizontally-scrolling `.hub-nav` tab row; and a `.landing-footer` under the tile grid, which doubles as R25/6e's landing contact-link placement (one footer, two links — "About · How this works" and a plain `mailto:` "Have feedback?" link — rather than two separate footers for two overlapping requirements). This was a deliberate call, not an oversight: R10's nav-tab overflow problem was already tight on phones with 5 tabs, and a 6th would have made it worse — see `TASK-PARALLEL-EXECUTION.md`'s "Cluster A" reasoning for why this and the nav-overflow fix below had to be decided together, once, rather than by independent guesses.

**Nav-tab overflow affordance (R10, Phase 6a-adjacent).** `.hub-nav`'s `overflow-x:auto` (unchanged) now pairs with a pure-CSS scroll-shadow: two "cover" gradients (`background-attachment:local`, scrolling with the tab row, hiding themselves once you've scrolled all the way to that edge) plus two "shadow" gradients (`attachment:scroll`, fixed to the viewport) that read as an edge fade whenever more tabs exist off-screen. Background-image only — no extra DOM element, so there's nothing that needs `pointer-events:none` and nothing that can block a tap on the last visible tab.

**About page provenance (R22, Phase 6b).** `hub-about` states what Dhanam is, a checkable privacy claim ("open devtools, watch the Network tab — you'll see no request leave this page," true because R5 already removed the only off-origin request the app ever made), a dated `.provenance-list` covering every statutory/market default in the app (motor-car perquisite, both tax regimes, IRDAI depreciation, `PROPERTY_STATES`, GST, the four rate defaults, Worth's projection defaults, Compare Cars' tier-2 group), a "known gaps" panel (B6's ~1% SIP timing-convention disagreement; every corpus/gain figure being pre-tax, per R32's "state-and-don't-model" assessment), and a build stamp + contact email. Per 6b's own instruction, the provenance list **disowns its defaults** — it never explains *why* 12% or 8.75% were chosen, only that they're starting points to replace, since explaining the reasoning would itself read as a recommendation. A `.provenance-row`/`-term`/`-value`/`-date` stacked list, not a table — a 3-column table squeezes the dated-prose values into unreadable narrow columns at 375px.

**Build stamp (`BUILD_STAMP`, R45/6e-ii).** A plain string constant near the top of the inline `<script>`, shown on the About page (`#about-build-stamp`, set once at init — `hub-about` has no `render*` function since it's static content, so this is a one-time `set()` call, not something that needs to survive a re-render). Deliberately distinct from `sw.js`'s `CACHE` string (that one only busts old caches and means nothing to a user); this one is what makes "I still see the bug" diagnosable on a cache-first PWA. Bump it **by hand** alongside `CACHE` whenever a user-visible change ships — neither is derived from the other, so both need editing.

- `hub-apartment` → **Dhanam Home** — property cost, home loan, and loan-disbursement (pre-EMI) calculators; the largest and most developed hub
- `hub-car` → **Dhanam Car** — a Loan/Lease/Cash financing-mode selector (Loan default) gating a company-car lease tax perquisite panel, plus Compare Cars (ICE vs EV cost-of-ownership across all three modes, absorbing the car-buying loan/depreciation detail as a per-car drill-down since Phase 14)
- `hub-sip` → **Dhanam Grow** — SIP planner (monthly / step-up / lumpsum sub-tabs via `switchSIPPlannerTab`)
- `hub-worth` → **Dhanam Worth** — net worth tracker: editable balance sheet, hero net-worth figure, change-since-last-update tile, a collapsed net-worth trend chart, a +5/+10/+20-year projection reusing `calcSIP`/`loanAtYear`, Excel export, and the backup/erase controls. The only hub that persists data — see **Persistence** below.

Within `hub-apartment`, secondary panels (`section-detail`, `section-loan`, `section-disb`) are shown/hidden via `toggleSection(id)` and `action-btn` toggle buttons, not separate routes or hubs. **They're an exclusive accordion (R14, Phase 3b):** opening one closes any other that's open, so at most one panel — and one result set — is on screen at a time; clicking an already-open panel's own button still collapses it (there's no other way back to a panel-free view). `toggleSection` lazily initializes each panel only the first time it's ever opened (`detailOpened`/`loanOpened`/`disbOpened` one-way latches) rather than on page load or on every reopen — collapsing and reopening a panel is a CSS class change only, never a re-render, so nothing typed into it is lost and `section-detail`'s prefill-from-quick-calc branch never runs a second time. This accordion is scoped to exactly these three panels; the nested `.collapse-card`s inside them (the loan/advanced/Buy-vs-SIP compare grids, the Worth trend card) are drill-downs within one task and are deliberately **not** part of it — a user comparing two of those at once is doing something legitimate. `openLoanCalc()`/`openDisbCalc()` route through the same `toggleSection()` call, so the deep-links get the sibling-collapse behaviour and the "second click doesn't collapse" behaviour together, with no second code path. `openPrepayCalc()` (R24, Phase 6d) extends the same pattern one level deeper: it opens `section-loan` via `toggleSection('loan')` exactly like `openLoanCalc()`, then additionally opens the nested `adv-section` ("Advanced: Extra Payments Projection") idempotently (`if (!el('adv-section').classList.contains('open')) toggleAdv();`) and scrolls *that* into view rather than the outer panel — `adv-section` sits deliberately outside the R14 accordion as a drill-down, so this must not, and does not, touch `sip-section` or any other nested card the user already has open.

**The Buy-vs-SIP comparison carries a caveat above its hero (R24's 6d-i, Phase 6).** `.sip-caveat`, inside `sip-section`'s `sip-body`, sits above `#sip-hero` — not inside the closed-by-default `#sip-compare-card` — specifically so a reader who looks only at the hero number still sees it. States the asymmetry plainly: prepaying/not taking the loan is a guaranteed, tax-free return at the loan's rate; the SIP side is uncertain and taxed on redemption; and the loan side also has its own ignored tax benefits (Section 24, 80C under the old regime), named so the caveat doesn't just trade one bias for its opposite. No tax rate is printed in it — rates are tier-2 and belong only in the About page's dated provenance list.

**State-aware stamp duty / registration (`q-state`, R21, Phase 6a).** The app used to hardcode Telangana's stamp duty (4%) and registration (0.5%) and present the resulting total with no regional qualifier — a confidently wrong number for anyone outside Telangana, with nothing on screen looking broken. A `<select id="q-state">` in Quick Estimate (defaulting to Telangana) now drives `q-reg` (the combined %) and, once `section-detail` is opened, the split `d-stamp`/`d-regfee` fields, via `applyPropertyState()`. Rates come from `PROPERTY_STATES`, a short, dated (2026-08-08), explicitly non-exhaustive table of 9 major states/UTs — single representative figures for a straightforward urban apartment purchase, not authoritative (real rates vary by buyer gender, property type, and municipal corporation even within one state). **All three fields (`q-reg`, `d-stamp`, `d-regfee`) stay directly editable after a state is picked** — the selector only sets a default. The active state's name is shown next to the figures it drives (`qr-state-lbl` in Quick Estimate's result row, `d-stamp-hint`/`d-regfee-hint` in the Detail panel) and flows into the PNG snapshot and Excel export headers too, so an exported document can't mislabel a Karnataka user's numbers as Hyderabad's. **Persists unconditionally as `DS.propertyState`** (a state code only, e.g. `'TG'` — never the rates, which stay tier-2 and always re-resolve from `PROPERTY_STATES`), the same "tier-1 fact about the user, not a market assumption" reasoning Compare Cars' shortlist already follows (B11) — re-picking your state every visit is exactly the friction that reasoning exists to avoid. Telangana is unchanged as the default, so an untouched selector reproduces every pre-R21 number exactly, and `resetAll()` resets it back to Telangana alongside its other field resets.

### Loan disbursement calculator (`section-disb`, inside `hub-apartment`)

For under-construction property loans where the bank disburses funds in stages and the borrower pays pre-EMI (interest-only) on the cumulative disbursed principal until the final tranche. This used to be its own top-level hub (`hub-disb`); it was folded into Dhanam Home as a third `toggleSection` panel, next to Detailed Cost Analysis and Loan Analysis, since it's a sub-topic of home buying rather than a distinct destination — reachable via the "Loan Disbursement (Pre-EMI)" action button or the landing page's `openDisbCalc()` deep-link tile.

- Inputs: sanctioned loan amount, rate, tenure, an editable tranche table (`disbTranches` — array of `{ pct, month }`, rendered by `renderDisbTranches()`, edited via `disbAddTranche()`/`disbRemoveTranche(i)`), and a "pay full EMI from day 1" toggle that skips the pre-EMI phase entirely.
- `renderLoanDisb()` is the single entry point: sums interest month-by-month on the disbursed-so-far principal up to the final tranche's month, then hands off to the existing `calcEMI()` for normal amortization over the given tenure. Warns (doesn't block) when tranche percentages don't sum to ~100%.
- **Do not call `renderDisbTranches()` from inside `renderLoanDisb()`.** `renderDisbTranches()` rewrites the tranche rows' `innerHTML`, which drops keyboard focus mid-typing if it runs on every keystroke (this was a real bug — fixed). It's only called on add/remove (`disbAddTranche`/`disbRemoveTranche`), on first open (`toggleSection`'s `disbOpened` branch), and from `resetAll()`. `renderLoanDisb()` itself just recomputes and updates the results table/summary from the current `disbTranches` state.
- A single 100%-at-month-0 tranche degenerates to a plain EMI loan and must match the `hub-apartment` loan panel's numbers exactly for the same principal/rate/tenure — useful as a sanity check when touching this code.

### Dhanam Car — financing modes (`carMode`, `car-mode-*`, Phase 14)

`hub-car` opens on a **Loan / Lease / Cash** selector (`setCarMode(mode)`, `car-mode-btn-loan/-lease/-cash`) — **Loan is the default**, not Lease. This exists because the app's own history showed the pattern: *"not every company offers a lease policy, but everyone aspires to buy a car"* (owner, 2026-08-09). Before Phase 14, `hub-car` opened directly on the lease tax-analysis panel, which demanded salary/bonus/EPF/regime before showing anything at all — a wall that told the ~80% of visitors without a lease policy the app wasn't for them in the first three seconds.

- **The lease panel is gated, not simplified — read this before touching `car-*`.** `setCarMode()` only toggles `#car-lease-panel`'s `display` (hidden unless `carMode === 'lease'`) and re-renders; not one field, hero, card, or caveat inside the panel changed shape for Phase 14. The car-loan EMI calculator that used to be a separate `cb-*` section is a commodity — every bank site has one — but the lease perquisite analysis is the single most *differentiated* thing in this app precisely because it's deep and niche. **Generalizing this hub meant adding the other two paths, never flattening the one that already existed** — a hub that became "just another EMI calculator" would have traded its only edge for reach it wouldn't actually get. If you're asked to "simplify" or "unify" the lease panel's fields with the loan/cash paths, that is exactly the failure mode this section warns against; gate visibility instead.
- **`calcOwnershipCost({ mode, ... })` (`calc.js`) is the shared engine — one function, three capital layers over the same running-cost/maintenance/insurance/depreciation core.** TCO is financing-agnostic: running cost, maintenance, insurance and depreciation are properties of the *car*; lease, loan and cash are capital layers on top. Renamed and generalized from Phase 9's `calcLeaseNetCost` (a real rename with a blast radius — every call site was updated, no compatibility shim kept):
  - `mode: 'lease'` — `emi = calcEMI(price, rate, N, residual)`; `capital = emi*months + residual`; `taxSaved = marginalRate * (emi − calcPerquisite(bigEngine, hasDriver)) * months`. Bit-identical to the old `calcLeaseNetCost` for the same inputs.
  - `mode: 'loan'` — `emi = calcEMI(price − downPayment, rate, N, 0)`; `capital = downPayment + emi*months`; `taxSaved = 0`.
  - `mode: 'cash'` — `emi = 0`; `capital = price`; `taxSaved = 0`.
  - All modes: `netCost = capital + runningTotal + maintTotal + insTotal − taxSaved`; `resaleValue = calcCarDepreciation(price, N)` — reveal-only in every mode, per Phase 13's rule, never a default headline line.
  - **`taxSaved = 0` for loan and cash is correct law, not an omission** — Indian tax law allows no deduction on a personal car loan or a cash purchase. The result cards and caveat list say so explicitly rather than leaving a blank line that reads as a bug.
- **`calcOwnershipCurve()` (R49, Phase 13) takes the same `mode` parameter** and produces the identical per-year cumulative-cost/car-value series across all three modes — loan expresses as the down payment landing in full at year 1 plus accumulating EMI, cash as the whole price landing at year 1 with no EMI, lease unchanged from Phase 13. Its endpoint equals `calcOwnershipCost`'s `netCost` in every mode (pinned in `tests.js`/`tests.html`).
- **`calcLumpsumGrowth(P, cagr, years)`** (`calc.js`) — `P * (1+cagr/100)^years` — backs the cash-mode opportunity-cost reveal below (B13). The Grow hub's `updateLumpsum` still computes this inline; it was not refactored to call the shared function (out of scope for Phase 14).
- **A first-time visitor in Loan or Cash mode never sees a salary/bonus/EPF/regime field** — `car-lease-panel`'s `display:none` default and `setCarMode()`'s gating are what enforce this; `renderCarCalc()` (the lease panel's own render function, below) only runs when Lease is selected.

### Dhanam Car lease analysis specifics (`renderCarCalc()`, `car-*` prefix)

`renderCarCalc()` compares three scenarios — baseline (own car), carve-out A (CTC reduced by the car package), additive B (car on top, perquisite added to taxable) — reading every input and writing the `#car-hero` and `#car-scenario-cards` (inside a closed-by-default `.collapse-card`) in one pass.

- **Hero + collapsed compare-card (R45, Phase 10a).** `#car-hero` (a `.total-card`) promotes whichever of Baseline/Carve-out A/Additive B currently wins — live and dynamic, recomputed on every `renderCarCalc()` call from the exact `aAdv`/`bAdv` comparison that used to feed the old `car-summary-box`'s "Best option" line, reused rather than recomputed a second way. The hero states the winner's effective net/mo plus the advantage in words ("Carve-out A saves you ₹9,360/mo over owning the car yourself"), and keeps the perquisite value/mo directly underneath as the one piece of context that explains *why* the scenarios differ. The three-card grid itself moved into a closed-by-default `.collapse-card` ("Compare all three scenarios ▾") at the same `#car-scenario-cards` spot — a markup change only, `buildCard()`'s output is untouched. `car-summary-box`/`#car-summary` was **dropped, not folded in**: every number it stated (perquisite, each scenario's effective net, the best-option verdict) is now on the hero or already on the card it restated, so keeping it too would state the same three numbers a third time.
- **`car-basic` is total fixed pay per month** (Basic + HRA + Special Allowance combined), not statutory Basic. Its `field-hint` says so; don't reintroduce copy that implies otherwise.
- **EPF is a directly-entered payslip figure (`car-epf-amt`), never derived from `car-basic` (R33/D14, Phase 8).** Real EPF applies to Basic + DA only, which the app has no way to infer from total fixed pay — the old `car-epf-pct × car-basic` derivation overstated the deduction by ~₹32K/month at upper-middle salaries and landed straight on the take-home figure. `epfMonthly`/`epfAnnual` are computed **once** at the top of the function; `scenarioCalc()`, scenario A, scenario B and the old-regime 80C cap all read those. That "once" matters: the bug existed in *three* independently-written copies of the same derivation, and only fixing the baseline would have left A and B wrong. Adding a fourth scenario means reusing `epfMonthly`, not writing a fifth derivation.
- **EPF is deliberately constant across all three scenarios.** A carve-out is taken out of the flexible/allowance side of the package, not out of statutory Basic, so the payslip's PF line doesn't move with it — and a constant EPF cancels cleanly out of every vs-baseline delta. Scenario A used to derive a lower EPF from `effectiveBasic`; that was the same bug in miniature. `effectiveBasic` legitimately still drives scenario A's *gross*, just not its EPF.
- **`calcPerquisite()` holds the Income-tax Rules, 2026 table** (R37, Phase 8b): ₹5,000/mo for ≤1.6L **or electric**, ₹7,000/mo above 1.6L, +₹3,000/mo for a chauffeur — the employer-meets-running-expenses row, which is the one this hub models. These replaced the Rules 1962 figures (₹1,800/₹2,400/₹900) on 2026-04-01 and sat stale in the code for four months. `perqAnnual` feeds Carve-out A and Additive B's taxable income only; Baseline never calls it.
- **Statutory constants rot silently — date them and pin them in the same commit.** Three separate bugs (R33, R34, R37) came from numbers that were right when typed. Anything touching a tax, duty, slab or perquisite value should (a) carry the notification/commencement date in a comment beside it, (b) state that date in the visible caveat copy, and (c) gain a test that pins the value, so a future edit has to be deliberate. `tests.js`'s `calcPerquisite` block is the worked example, including an assertion that no superseded value survives anywhere in the table.
- The panel's caveat list is the disclosure surface for every simplification here (no professional tax, no old-regime HRA exemption, the EPF model above, the perquisite figures' effective date). Add to it when you add an assumption — this hub's numbers are compared against real payslips.

### Dhanam Car — Compare Cars specifics (`renderCarCompare()`, `cc-*` prefix, Phase 9, financing modes since Phase 14)

A third stacked section in `hub-car`, below the single-car lease analysis panel (gated to Lease mode) — it answers "which car?" and, since Phase 14, "financed how?" across Loan/Lease/Cash. Deliberately **not** wrapped in `toggleSection()`'s accordion (that's scoped to `hub-apartment`'s three panels); `hub-car` has no accordion and this doesn't introduce one. Ported from an owner-built Excel workbook (`carlease/`), with two modeling bugs in that workbook fixed rather than carried over — see the next two bullets.

- **Engine: `calcRunningCost`, `calcInsuranceTotal`, `calcOwnershipCost`, `calcBreakevenKm`, `calcOwnershipCurve`, `calcLumpsumGrowth` in `calc.js`**, plus `calcEMI`'s optional fourth argument `fv` (balance still owed at term end — a lease's residual/balloon buyout, so the EMI only amortizes `price − PV(residual)`, not the full price; `fv = 0` in loan/cash mode). All pure, all covered by `node tests.js`. `calcOwnershipCost` (renamed and generalized from Phase 9's `calcLeaseNetCost` in Phase 14/R55 — see **Dhanam Car — financing modes** above) is the one function that ties them together — `netCost = rawOutflow − taxSaved`, `taxSaved` stays **signed** in lease mode (an EMI below the perquisite is a genuine negative, not a bug to clamp away) and is exactly `0` in loan/cash mode (no deduction on a personal car loan or cash purchase under Indian law).
- **Resale credit is computed but no longer shown by default (R40, then superseded by R50, Phase 13; unchanged in Phase 14 — mode-independent).** The source workbook counted the lease-end buyout as pure cost and never credited the car you then own — on a ₹20L car at a 10% residual over 4 years, `calcCarDepreciation` puts that car at ₹9.83L against a ₹2.00L buyout, a ₹7.83L credit the workbook dropped entirely, and because it scales with price it reorders the ranking rather than just inflating every total equally. R40 fixed that by computing and showing both `netCost` (before) and `netCostAfterResale` (after) on every card. **The owner then pushed back on the credit itself (2026-08-09, Phase 13):** `calcCarDepreciation` is an IRDAI schedule for pricing motor insurance (insured declared value), not a market resale predictor, and a single ICE-shaped curve applied to EVs as well — a forecast, not a fact, and it shouldn't sit in a default headline next to cash actually spent. Both fields are still computed and returned by `calcOwnershipCost` (still tested, still real) in every mode — `resaleValue = calcCarDepreciation(price, N)` never depends on `mode` — but the per-card detail no longer states them by default; see the next two bullets.
- **Ranking uses net cost — money out the door, full stop (R50, Phase 13; retires B12).** `ccComputedRows()` sorts on `.result.netCost`, computed under whichever mode `carMode` currently selects. B12 (2026-08-07, "rank on netCost before the resale credit") is retired: there's no longer a competing after-resale figure sitting on every card for the ranking to justify itself against, since that figure moved behind the R51/R52 reveal below. A `.sip-caveat`-styled disclosure (`#cc-caveat`, same placement reasoning as the Buy-vs-SIP caveat — see `hub-apartment` above) sits above `#cc-hero` stating the consequence in words: this default favours cheaper cars over ones that hold their value, since resale value scales with price and the ranking ignores it (a ₹7.5L unrecognised-asset gap between a ₹12L and ₹30L car at 5 years — far larger than the running-cost gaps the ranking is actually built on).
- **Loan mode adds a per-car down payment (R57, Phase 14).** `ccRowHtml()` renders a "Down Payment (₹)" field per shortlist row only when `carMode === 'loan'` (same show/hide-not-grey rule as the mode selector itself); stored as `c.downPayment` on the `ccCars` array element, defaulting to `0`. `ccOwnershipInput()` — the single place that builds `calcOwnershipCost`/`calcOwnershipCurve`'s input object from a car + the shared `ccAssumptions()` — reads it as `c.downPayment || 0`, so a car added before switching to Loan mode still computes correctly at ₹0 down.
- **The opportunity-cost reveal (R58, answers B13).** Charging cash mode an opportunity cost on the full price while loan/lease pay nothing on their own tied-up capital would rig the comparison against cash — B13 chose option (c): an explicit reveal, off by default, applying the *same* `calcLumpsumGrowth()`-based formula to whatever capital each mode ties up upfront (full price in cash, the down payment in loan, ~₹0 in lease — a lease's structural lack of upfront capital is a real finding, not an inconsistency). `ccOpportunityCost(mode, price, downPayment, cagr, years)` computes `calcLumpsumGrowth(capital, cagr, years) - capital`. Surfaced two places, both tier-3/unpersisted like the R51/R52 resale reveal: the per-car detail view's own "Reveal opportunity cost of tied-up capital" button (`ccOppRevealOpen`), and the R60 cross-mode comparison's shared reveal below. **The per-car button is visible in all three modes, not just Cash** — an early build pass gated it to Cash-only, which was itself a B13 violation (a Loan-mode user's down payment never got the same question asked of it that a Cash buyer's full price did); found in code review, fixed before ship. The button's label is mode-aware (`oppCapitalLabel` in `renderCCOwnCurve()`) so it always names the actual capital being charged.
- **`calcOwnershipCurve()` (calc.js, R49, Phase 13; generalized to all three modes in Phase 14/R55)** is the enabler for the reveal below: `{ years, cumulativeCost, carValue }` for one car over its full term, both series accumulated year-by-year from the *same* capital/running/maintenance/insurance/tax-saved terms `calcOwnershipCost` sums once at the full term. The EMI is computed **once**, at the full term (`o.years`) — never re-derived per year by calling `calcOwnershipCost` with a shorter `years`, which would price a *different* lease/loan at each point and silently disagree with the card's headline number. Two invariants hold by construction (pinned in `tests.js`/`tests.html`) in **every mode**: `cumulativeCost[N−1] === netCost` (the curve's endpoint **is** the card's headline) and `cumulativeCost[N−1] − carValue[N−1] === netCostAfterResale` (the gap **is** the reveal figure) — so the chart never needs a second model, only a second reading of the first one. In lease mode the cost curve's final year still steps sharply (the residual buyout lands there); loan mode's down payment lands in full at year 1 instead, and cash mode's whole price lands at year 1 with a flat (no-EMI) climb thereafter.
- **Cost-vs-value chart + resale reveal (`renderCCOwnCurve()`, R51/R52, Phase 13; mode-aware since Phase 14).** A default-**open** `.panel-card` (`#cc-owncurve-card`, deliberately not a `.collapse-card` like `cc-chart-card` below it — this is the default view now, not a drill-down), showing one car at a time via a `<select>` (`#cc-owncurve-select`, defaulting to rank 1; `onchange` re-renders just this card, the same "discrete committed choice" reasoning as the shortlist row's own Type select). Two series on the shared `renderChart()` helper, no area fill (the crossing is the payload — a fill would obscure it): cumulative cost in `--accent` (rising), car value in `--text-dim` (falling), using the existing `.chart-legend`/`.chart-legend-item`/`.chart-legend-dot` pattern (already used by the loan and SIP-growth charts) rather than inventing a new legend convention. **The chart's host (`#cc-owncurve-chart`) is a static sibling in the page's initial markup, never generated inside `#cc-results`' innerHTML template** — that template rewrites on nearly every keystroke elsewhere in the section (assumptions, other rows), and a chart host recreated that often would tear down and rebuild a `ResizeObserver` every keypress (the R20 anti-pattern). The same static-host rule applies to the absorbed `#cc-depr-chart` below it. The resale reveal button shows `netCostAfterResale` for the selected car behind a **permanent, adjacent text label** ("a forecast, not part of the ranking above") — never a bare styled number, since an exported screenshot could otherwise read it as the real hero (there is no Compare Cars export today, but the rule is written down for the first one that lands here). **Both reveals' open/closed state (`ccRevealOpen`, `ccOppRevealOpen`) are tier-3 UI state — plain JS variables, never written into `DS`/`dhanam.v1`**, the same rule R25's orientation-line dismissal follows: they always start closed on a fresh load. The selected car (`ccOwnCurveIdx`, keyed like `ccForceOpen`/`ccForceClosed` by the row's stable index into `ccCars`) is reset to `null` (falls back to rank 1) by `ccRemoveCar()` and `hydrateCC()`, for the same index-shift reason those already reset the R47 disclosure Sets.
- **Absorbed car-loan detail view (`ccRenderLoanDetail()`, R59, Phase 14).** The standalone `renderCarLoan()`/`cb-*` "Car Buying — Loan Analysis" section (Phase 10b/R46, Phase 13/R53) was retired and its three capabilities re-homed inside the per-car detail view, visible only in Loan mode, keyed off the selected car's own price and down payment: (1) the 5-year-hero-equivalent **3/5/7-year tenure grid** (`#cc-tenure-card`, closed by default) reusing the exact same `tenureStats(yr)` helper carried across intact — not re-derived; (2) the **IRDAI depreciation table + 7-year resale curve** (`#cc-depr-section`/`#cc-depr-chart`), mode-independent and rendered off `price` alone (R13's original rule) for every mode, not just Loan; (3) **R53's outstanding-loan-balance overlay** on that same chart — a second `--red` series from `loanAtYear(loan, rate, a.years, y)`, added only when `carMode === 'loan'` and a loan amount `> 0`, sliced to `cc-years`' actual selected term (3/4/5yr — never the chart's full 7-year window, which would draw a flat post-payoff zero-balance line), with the underwater note still reporting the actual first-through-last underwater year range in words, never "from year N onward". **`loanTermYears` reads the section's own `cc-years` selector, not a hardcoded constant** — a post-Phase-14-review fix: the migration from `cb-*` initially carried over R53's literal `5`, whose justification ("no separate chosen-tenure input exists here") stopped being true the moment R59 gave Compare Cars its own term selector already driving the EMI/netCost/ranking above it; a fixed 5-year line disagreed with, and could print a false "underwater" claim for, a real 3- or 4-year loan. Both original Phase 13 bugfixes (tenure-sliced balance series, actual-range underwater wording) still hold — the overlay's own guard is deliberately just `loan > 0` (not `&& rate > 0`), matching the retired code's behavior at a 0% rate (`loanAtYear`'s zero-rate branch is a valid, non-NaN linear amortization).
- **Cross-mode comparison (R60): lease vs. loan vs. cash for the SAME car, side by side.** `renderCCCrossMode(car, a)` (called from `renderCCOwnCurve()`, so it always reflects the same selected car and shared Assumptions-card inputs as the chart above it) calls `calcOwnershipCost()` three times — once per mode — for that one car, using the car's own down payment for the loan column and the shared rate/term/marginal-rate/driver assumptions for all three, regardless of which mode currently drives the ranking. This is the payload that justifies Phase 14's whole restructure: TCO is financing-agnostic, so once all three modes share one engine, the same car's three capital-layer costs become directly comparable. One shared reveal (`ccCrossRevealOpen`, tier-3, unpersisted) applies the resale credit **and** the opportunity-cost forecast identically to all three columns at once — B13's fairness rule made concrete: never a friendlier default treatment for one mode's column than another's. **`#cc-crossmode-assumptions` states the Lease/Cash-only inputs the card is actually using, in words, regardless of active mode** (post-review fix) — because `updateCCModeFields()` only ever shows ONE mode's field group at a time, a Loan- or Cash-mode visitor otherwise had no way to see the residual %, marginal tax rate, or driver flag driving the Lease column's number (which, via the marginal rate alone, can make Lease look unconditionally cheapest for a reason nobody typed in or can see) — this line is the same "state the assumption in words" instinct as the resale reveal's permanent label, applied to inputs rather than an output.
- **Perquisite comes from `calcPerquisite()`, never a flat value (correction 2, R39) — lease mode only.** The workbook hardcoded ₹5,000/month for every car, which overstates tax saved on a >1.6L ICE car by ~₹30K over 4 years and erases the structural EV advantage (EVs are always in the ≤1.6L bracket regardless of motor size). Each ICE row in the shortlist has its own "Engine > 1600cc" checkbox (`bigEngine`, hidden and forced `false` for EV rows); "Company Driver Provided" (`hasDriver`) is a single Assumptions-card toggle, not per-car — a driver is the user's own choice, not a property of the car. Both fields are shown/hidden with the rest of the lease-only Assumptions fields per `updateCCModeFields()` (R57's "show/hide, not grey out" rule).
- **The blank-row guard reads raw input state, not a derived/truthy value (workbook §8; this codebase's own R16 already shipped this exact bug once).** `ccComputedRows()` filters on `c.price > 0 && c.eff > 0` straight off the `ccCars` array — a row participates only once both are genuinely filled in. Since a real car never legitimately costs ₹0 or does 0 km/kWh, this doubles as "not blank" with no separate blank/zero distinction needed (unlike `su-stepup`'s R16 case, where 0 was a legitimate value).
- **`renderCCRows()` never runs from a text/number field's `oninput`** (same D6/Phase-0 trap `section-disb`'s tranche rows already hit): rewriting the row inputs' `innerHTML` on every keystroke drops focus mid-type. It does run on add/remove/hydrate, and on a row's own Type `<select>`'s `onchange` — a discrete, already-committed choice rather than a keystroke, and one that has to rebuild the row anyway since the Mileage/Efficiency label text, the `>1600cc` checkbox, and (in Loan mode) the down-payment field are baked into `ccRowHtml()` at render time. Every other per-field handler calls `renderCarCompare()` directly, which only ever rewrites `#cc-results`/`#cc-hero`/`#cc-breakeven-rows`/the charts — never `#cc-rows`.
- **Breakeven-km hero** (`calcBreakevenKm`, 9d) needs at least one ICE row and one EV row to render — hides otherwise. `fixed` cost for each car is derived from its already-computed `calcOwnershipCost` result (`netCost − runningTotal`), never recomputed independently, so it stays a single source of truth with the result cards above it. Three distinct outcomes render as sentences, never a bare number: `null` → "never breaks even", `<= 0` → "already cheaper today", otherwise → "Needs N more km/yr".
- **Crossover chart** (`renderCCChart()`, 9e) sweeps a uniform 0→30,000 km/yr axis (chartSvg spaces by index, not value — R17's rule applies again here) and colors the cheapest car's line gold to match its rank-1 card; every other line uses a neutral shade (`--text`/`--text-mid`/`--text-dim`/`--border`), never green/red, since those stay reserved for a real financial delta (palette rules 1/3/4) and this is just line differentiation.
- **Persists by design, no opt-in — the first calculator hub to do so** (owner decision, B11, 2026-08-07; see the persistence section below). Tier-1 only: `ccCars` and the two driving-pattern fields (`cc-city-km`/`cc-hwy-km`). Every other Assumptions-card field (lease rate, residual %, petrol/charging prices, maintenance, insurance, IDV depreciation, marginal rate) is tier-2 and is read fresh from the DOM on every render, never persisted.
- **Per-card disclosure — a second, distinct collapse pattern from the section-level `.collapse-card`s (R47, Phase 10c).** Each `.cc-rank-card` shows a compact default — rank badge, name, one hero number (net cost, large), one supporting line (`EMI ₹X/mo · ₹Y/km`) — with the rest (raw cash outflow, EMI/month, signed tax saved, blended cost/km, vs-cheapest) behind its own nested `.collapse-card` (`cc-detail-{r.i}`, toggled via `ccToggleDetail(i)`, not the shared `toggleCard()`). **Resale credit and net cost after resale are no longer in this list at all (R50, Phase 13)** — they moved to the R51/R52 reveal above, which shows one car at a time rather than restating the figure on every card. Where `car-compare-card`/`cb-compare-card`/`cc-assumptions-card` each collapse *a whole feature*, this collapses *individual list items* — rank-1 defaults open, ranks 2+ default closed, and each card's state is independent (`ccForceOpen`/`ccForceClosed` Sets, keyed by the row's stable index into `ccCars`) rather than an accordion: opening one card never closes another. A user's own toggle overrides the rank-based default and must survive the next `renderCarCompare()` call, since `#cc-results`' innerHTML gets rewritten on nearly every keystroke elsewhere in the section (assumptions, other rows) — that's why the state lives in `ccToggleDetail()`'s Sets and not just as a CSS class baked into the template. `ccRemoveCar()` and `hydrateCC()` both clear both Sets, since splicing/replacing `ccCars` invalidates the index-to-car mapping the state was keyed on.

### Car Buying — Loan Analysis (retired, Phase 14/R59 — absorbed into Compare Cars)

Through Phase 10-13 this was the third and last `hub-car` section (`renderCarLoan()`, `cb-*` prefix): a plain car loan (down payment / rate / tenure) plus an IRDAI-depreciation resale table with an outstanding-loan-balance overlay (R53, answering B8), unrelated to the lease scenarios above it. **Owner decision, 2026-08-09 (Phase 14): no duplication** — a car-loan EMI calculator is a commodity every bank site already has, and shipping it as a second, separate answer to "what's my car loan EMI?" next to Compare Cars' own loan-mode cost path was exactly the kind of thing that quietly rots. `renderCarLoan()`, its `cb-*` markup/ids, its `switchHub()` call, and its `resetAll()` handling were deleted; all three of its genuinely useful capabilities — the down-payment concept, the 3/5/7-year tenure grid (`tenureStats()` carried across intact, not re-derived), and the IRDAI depreciation table/resale curve with R53's loan-balance overlay — were migrated first and verified against the live `cb-*` output number-for-number, **then** deleted (a migration, not a subtraction). They now live in Compare Cars' per-car detail view, visible only in Loan mode — see `ccRenderLoanDetail()` in **Dhanam Car — Compare Cars specifics** below. No live `cb-*` DOM id remains anywhere in `index.html`; the string still appears in a handful of code comments that explain this history, which is deliberate — grep for `id="cb-` (not bare `cb-`) to confirm.

### No backend; persistence is local-only and deliberately narrow

There are no API calls, no database, and nothing ever leaves the device. Every input has an `oninput`/`onchange` handler that recalculates and re-renders synchronously (no debouncing, no async).

Since Phase 2a there *is* a small `localStorage` layer, and the rules around it are strict:

- **One versioned key**, `dhanam.v1`, plus a `dhanam.seen` marker used to detect browser eviction. `saveState()`/`loadState()`/`eraseState()` are the only entry points.
- **Only tier-1 data is stored** — facts about the user (balances, their loan's numbers). **Never store tier-2 market or statutory assumptions** (the 8.75% rate default, stamp duty %, tax slabs, IRDAI depreciation): they must reload from code every time, or a stale saved value silently outlives reality. A field that has a meaningful app default (like `l-rate`) is persisted *only when the value differs from that default*. Full rationale in `UX-ANALYSIS.md` §2.1.
- **Reads and writes never throw.** A corrupt, foreign, or wrong-version blob is treated as *absent* (`storageUnreadable`), a failed write sets `storageFailed` so the UI never claims data is safe when it isn't, and hydration runs **after** first render so a bad blob can't block paint. This matters more than usual: it's the only bug class in the app that a reload can't rescue the user from.
- **The schema is flat and additive** — unknown keys ignored, missing keys defaulted. Only bump `STORE_VER` for genuinely breaking shape changes, and write a real migration when you do; never silently discard a user's balance sheet.
- **`history` is append-only**, one `{date, netWorth}` entry per save-day (same-day saves overwrite), capped at 120. It exists so the change tile and the trend chart have something to compare against — history cannot be reconstructed retroactively, so don't drop it.
- Calculator hubs persist nothing unless the user opts in via the loan panel's "Remember my inputs on this device" toggle (default off; switching it off deletes the stored inputs). `hub-worth` persists by design — a balance sheet you retype every visit is worthless. **Compare Cars (`hub-car`'s `cc-*` section) persists by design too (B11, Phase 9)** — the first *calculator* hub to do so — on the same reasoning: a shortlist of dealer-quoted cars is closer to a balance sheet than to a scratch calculation. Only `ccCars` and the two driving-pattern fields are stored; every other Compare Cars input is tier-2. See **Dhanam Car — Compare Cars specifics** above.
- Users can **download a JSON backup, restore one, and erase everything**. Imports are validated exactly as `loadState()` validates. This is the only way to move data between devices; there is no sync and adding one would require a backend (`UX-ANALYSIS.md` §2.5).
- **`worthSnapshot()` (R65, Phase 16) is the one and only place outside `hub-worth`'s own code that reads `DS.worth`.** `ARCHITECTURE-ANALYSIS.md` §2 warned specifically about a calculator reaching into another hub's state via shared globals with nothing enforcing the boundary — this function *is* that boundary. It reads the persisted blob, not the DOM (`buildWorth()` only creates the `w-a-*`/`w-l-*` inputs the first time `hub-worth` is opened this session, so a user with a saved balance sheet who hasn't visited Worth yet this session has no `w-a-*`/`w-l-*` elements to read `v()` from at all — reading `DS.worth` sidesteps that entirely). Returns `null` — never a fabricated ₹0 — when there's nothing real saved (no `DS`, no `DS.worth`, or every stored figure is zero); otherwise returns `{ assets, liabs, net, aVals, lVals, investable, propertyVal, cagr, debtRate, debtYears, monthlySip }`, falling back to the app's maintained projection defaults (`PROJ_DEFAULT_CAGR`/`PROJ_DEFAULT_DEBT_RATE`/10 years) for any tier-2 assumption the user never touched, exactly as `renderWorthProjection()` itself would. Read-only — never writes `DS`. **Its one consumer today is `renderAdvWorthBridge()`** (the reverse Worth bridge, see below); any future feature wanting the user's real balance sheet must call `worthSnapshot()` too, not read `DS.worth` a second way.

### Client-side error visibility (R66, Phase 16)

`ARCHITECTURE-ANALYSIS.md` recommendation #2, open since day one: before Phase 16 the only `onerror` anywhere in the app was `reader.onerror` on the backup-file input, so a stranger who hit a JS exception saw a silently dead calculator with nothing to report — as unfalsifiable as "I still see the bug" was before `BUILD_STAMP` existed. This is app-wide infrastructure, not scoped to any one hub, so it lives as an IIFE at the very top of the inline `<script>` — installed before `BUILD_STAMP` and everything else, so it covers init-time failures too, not just ones after the page has loaded.

- **`window.onerror` and `window.addEventListener('unhandledrejection', …)`, both** — a rejected promise never reaches `window.onerror`, so one alone isn't enough.
- **Never sends anything anywhere — this is the one non-negotiable constraint.** No `fetch`, no `XMLHttpRequest`, no `navigator.sendBeacon`, no tracking pixel, not even to a "free error-logging" third party. It only ever writes text into a dismissible on-screen panel (`#err-panel`, appended to `document.body` on first error) for the user to read and copy by hand — the same shape and reasoning as the local feedback composer (R25/6e-i). An error-reporting endpoint would forfeit the About page's *"open devtools, watch the Network tab — you'll see no request leave this page"* claim more cheaply than a whole backend would. Verified after shipping: `grep -n "fetch(\|XMLHttpRequest\|sendBeacon\|new Image(" index.html` matches nothing but the comment stating this rule.
- **Its own dismissible element, not `showToast()`.** The existing toast auto-hides after 3.2s; diagnostics need to stay on screen long enough to read and copy, so `#err-panel` is a separate fixed-position panel with its own close button, not a reuse of `#toast`.
- **The panel states `BUILD_STAMP`, the error message, source/line/column, and the stack when available**, plus a "Copy diagnostics" button (`navigator.clipboard.writeText`, with a `document.execCommand('copy')` fallback for browsers without the Clipboard API).
- **Deduped, not stacked.** The same error repeating (e.g. thrown from inside a `render*` function called on every keystroke) updates the existing panel and a `(×N)` repeat counter instead of piling up a new panel per throw; a genuinely different error replaces it and resets the counter.
- **The handler can never itself throw or loop.** Every DOM-touching step is wrapped in its own `try/catch`; if building the panel itself fails, it falls back to a single `window.alert` and gives up — it never re-enters `window.onerror` or re-throws.
- **`--red` is used for the panel's header/border** — a real app failure, not a financial delta, but the task that specced this explicitly permits it; still exactly one hue outside the three-hue rule's normal scope (gold/green/red), no fourth colour introduced.
- Does not touch `calc.js` — this is pure UI infrastructure, not a calculation.

### Keyboard & ARIA accessibility (R8, Phase 17)

D8/`ARCHITECTURE-ANALYSIS.md`'s accessibility gap, re-rated Medium → High once B4 answered "general audience": before Phase 17 there were zero `aria-expanded`, `role="tablist"`, `aria-selected`, or `tabindex` occurrences anywhere in the file, and every collapse header was a clickable `<div>`, not a `<button>`. This is an app-wide sweep, not scoped to one hub.

- **Every `.collapse-header`/`.adv-header`/`.sip-header` is now a real `<button type="button">`, not a `<div onclick>`.** `grep -n '<div class="collapse-header"\|<div class="adv-header"\|<div class="sip-header"' index.html` returns nothing — treat a nonzero result as a regression. New CSS resets on those three classes neutralize native `<button>` chrome (background/border/font/width/text-align) so none of them visibly changed shape.
- **`toggleCard(id)` sets `aria-expanded` on the card's own `:scope > .collapse-header` button in one place** — this is what keeps every `toggleCard()`/`toggleChartCard()` caller correct for free. `toggleSection()`, `toggleAdv()`, `toggleSIP()`, and `ccToggleDetail()` each set `aria-expanded` explicitly, since none of them route through `toggleCard()`. **Any new collapse/expand control must do the same** — either call `toggleCard()` or set `aria-expanded` itself; a `<button>` with no `aria-expanded` update is a silent regression of this pass, not a wash.
- **Tab semantics on both tab bars.** `role="tablist"` on `.hub-nav-inner` and `.sip-planner-tabs`; `role="tab"` + live `aria-selected` on every `.hub-tab` and `.sip-planner-tab`, kept current by `switchHub()`/`switchSIPPlannerTab()`.
- **The car financing mode selector (`car-mode-btn-loan/-lease/-cash`) deliberately uses `aria-pressed`, not `role="tab"`/`aria-selected`.** It's an exclusive toggle-button group choosing one setting ("how are you paying"), not navigation between panels of otherwise-unrelated content the way the hub/SIP tab bars are — `role="group" aria-label="How are you paying for the car?"` wraps the row, `setCarMode()` keeps `aria-pressed` live. Any future segmented-control-style toggle (not a navigational tab bar) should follow this pattern, not the tablist one.
- **44px touch targets** via one `@media(max-width:600px)` rule covering `.hub-tab, .sip-planner-tab, .action-btn, .btn, .collapse-header, .adv-header, .sip-header` — deliberately excludes `.tile` (already sized generously) and `.mode-btn` (the small ₹/sft-vs-lump inline widget, not a tab/mode selector).
- **One global `:focus-visible` ring** (`a, button, input, select, textarea, [tabindex]`), reusing the existing gold `--accent` ring the file's three pre-existing rules already established — no new color introduced.
- **Accessible-name gaps filled on already-existing controls**: `#l-remember`'s checkbox (its visible label text lives in a sibling `<div>`, outside the wrapping `<label>`, so it previously had no accessible name at all) got an explicit `aria-label`; `#w-hide-btn` got a live `aria-pressed` from `toggleHideAmounts()`; `#w-import-file` (the restore-backup file input) got `aria-label="Restore backup file"`; the icon-only "✕" remove buttons in `disbRemoveTranche()`/`ccRemoveCar()` got `aria-label` alongside their existing `title`.
- **Known remaining gap, out of this pass's scope on purpose:** the header logo's `.header-inner.clickable` div (the "tap the logo to go home" affordance) is still not keyboard-reachable. Left as-is because it's redundant with the already-accessible `⌂ Home` nav tab — not a silent omission, a scoping call, recorded here so a future full-coverage pass knows it's the one open item.
- Does not touch `calc.js` and changes no financial output — `node tests.js` stayed at 100/100 throughout.

### Inline term definitions (R23, Phase 18)

The highest-value comprehension work identified in `TASK-UX-REDESIGN.md`'s Phase 6c: inputs are self-explanatory, but outputs like "perquisite value," "pre-EMI," or a projection that silently holds property flat are not, and misreading one is a worse failure than not knowing which button to press — one the user won't notice on their own. Definitions go **at the term**, not in a collected glossary (recognition beats recall).

- **One reusable component** — `.term-info`/`.term-info-btn`/`.term-info-pop`, `toggleTermInfo(id)` — placed at 17 terms across every hub, not twelve-plus bespoke tooltips. The trigger reuses the existing `#i-info` `<symbol>` (same icon the About-page link and caveat headers already use) at `--text-dim`, a fixed neutral rather than `currentColor`-inherited — same reasoning as `.tile-icon`/`.ab-icon`'s pin in the Icons section: this glyph should read as quiet, always-available help, not compete with whatever active/gold state its surrounding label might carry.
- **Tap-friendly, not hover-only** (mobile-first Android audience) — the glyph is a real `<button type="button">`, not a styled span. **Reuses R8's `aria-expanded` convention on the trigger exactly** — a deliberate choice not to invent a second "is this open" pattern alongside R8's collapse-header one.
- **Only one popover open at a time.** `toggleTermInfo(id)` closes whichever other one is open before showing the requested one — `openTermInfoId` is the single source of truth, mirrored by exactly one `[aria-expanded="true"]` trigger at any moment.
- **Dismissible three ways**: re-tapping the same trigger, clicking/tapping outside (a document-level `click` listener checks `.contains()` against both the popover and its trigger), or Escape (which also returns focus to the trigger button — the standard disclosure-widget contract, so a keyboard user isn't left with focus on a now-invisible element).
- **`position:fixed`, not `position:absolute` — deliberately.** Nearly every placement sits inside a `.panel-card` or `.collapse-card`, and both set `overflow:hidden` (see the Styling section's D7/R62 history for another example of a rule that looked locally correct and clipped something two files away). An absolutely-positioned popover anchored inside either would be silently clipped the moment it extended past that ancestor's box. Fixed positioning escapes the containing-block chain up to the viewport, so it can't be clipped by an ancestor's `overflow:hidden` — the tradeoff is that `toggleTermInfo()` computes `top`/`left` in JS from the trigger's `getBoundingClientRect()` at open time (clamped to stay inside the viewport horizontally, and flipped to open upward if there's no room below) rather than letting CSS position it declaratively.
- **Closes on scroll or resize instead of repositioning.** A fixed-position element doesn't move with the page, so a scroll would leave it visually detached from the trigger it's meant to explain; closing it is simpler and safer than re-running the position math on every scroll event.
- **Placement judgment calls, recorded so a future duplicate-vs-skip decision has precedent**: CAGR appears at its first/primary occurrence per hub (`sip-cagr` in `hub-apartment`'s Buy-vs-SIP card, `sp-cagr` on Dhanam Grow's Monthly tab only — not repeated on Step-up/Lumpsum, `w-proj-cagr` in Dhanam Worth's projection card) rather than at every field sharing the concept (`su-cagr`, `ls-cagr`, `cc-cash-cagr` were left undecorated). "Corpus" is defined once, on Grow's Monthly tab, not on all three. The Tranche definition renders only on the first tranche row (`i === 0` inside `disbTrancheRowHtml()`'s template) — one definition, not one per row, and it avoids a duplicate-id bug a naive per-row placement would have introduced.
- No new hue (palette rule 1); does not touch `calc.js` — pure UI/markup, no calculation changed.

### Dhanam Worth specifics (`hub-worth`, `w-*` prefix)

`renderWorth()` is the single entry point: reads all rows, totals both sides, writes the hero figure, updates the change tile, and persists. Rows are generated from the `W_ASSETS`/`W_LIABS` arrays by `buildWorth()` on first open (`worthBuilt` flag) — add a category there, not in the markup.

- **The change tile** compares against the most recent history entry from a *different* day, so today's own running entry is never its own baseline. It renders nothing at all on a first visit, a neutral "No change" at zero, and `--green`/`--red` only for a real delta — always with an arrow glyph *and* the words "Up"/"Down", so colour is never the only signal (palette rule 5).
- **Empty state** shows `—`, never `NaN` and never a fabricated ₹0 net worth; nothing is written to storage until at least one field is non-zero, so an empty first visit can't seed a bogus baseline for tomorrow's delta.
- **Negative net worth** renders with a `−` prefix and turns the hero red (a real negative financial position, palette rule 4).
- **"Hide amounts"** adds `.amounts-hidden` to `#worth-main`, which blurs every figure. If you add any element that displays a number or a share, add it to that selector too — the per-row `%` shares, the change tile's "was ₹X" basis line, the projection card's inputs, and the trend-chart SVG itself all had to be included.
- **Trend chart** (`renderWorthTrend()`) — a `.collapse-card`, closed by default, rendering `DS.history` through the shared `chartSvg()` helper (see **Charts** below). Hidden entirely with zero history, a friendly "not enough history yet" message with one point, a real line/area chart from two points up.
- **Projection bridge** (`renderWorthProjection()`) — a "Projected Net Worth" panel-card reusing `calcSIP` (investable-asset growth plus an ongoing monthly-savings input, `w-proj-sip`) and `loanAtYear` (liabilities amortizing from today's outstanding balance over a user-given rate/remaining-years) to project net worth at +5/+10/+20 years. Property is deliberately held flat — this hub has no basis to assume an appreciation rate. The two rate assumptions (`w-proj-cagr`, `w-proj-debt-rate`) follow the same tier-2 "persist only if changed from default" rule as `l-rate`; the monthly-savings figure and years-to-debt-free are tier-1 facts and always persist. Hidden entirely until the balance sheet has at least one non-zero field.
- **Excel export** (`buildWorthRows()` / `exportWorthExcel()`) follows the `buildDetailRows()`/`buildLoanRows()` pattern — see **Excel export** below.
- **`calcNetWorthProjection(investable, propertyVal, liabilities, cagr, debtRate, debtYears, monthlySip, years)` (`calc.js`, R65, Phase 16)** holds `renderWorthProjection()`'s own formula, extracted so it and the reverse Worth bridge below can never independently drift into two different net-worth projections — both call this exact function with the exact same argument shapes. `renderWorthProjection()`'s local `projectedNet` closure is now a one-line call into it; behaviour is bit-identical to the pre-R65 inline version for the same inputs (it's a refactor, not a new model).

**Reverse Worth bridge (R65, Phase 16) — the narrow half only.** `UX-ANALYSIS.md` §Strategic-2 promised two things that were never built: (1) *"the prepayment simulator can show its effect on your net worth curve, not a hypothetical loan"*, and (2) *"the verdict cards become personal advice instead of generic comparisons."* Phase 16 builds (1) and **explicitly declines (2)** — see `UX-ANALYSIS.md` §Strategic-2 for the recorded decision and reasoning (in short: this app has consistently refused to give advice — the Buy-vs-SIP `.sip-caveat` says *"not a verdict"* in as many words, and About-page checklist item 42 requires no sentence read as a recommendation — and turning comparison cards into personal advice is an escalation in kind, not degree, that would contradict that shipped stance).

- **`renderAdvWorthBridge(interestSaved)`**, called from `renderAdvLoan()` (`hub-apartment`'s `adv-section`, "Advanced: Extra Payments Projection"), is the sole consumer of `worthSnapshot()`. It shows what the on-screen extra-payments scenario (the same 20-year-hero convention `renderAdvLoan()` already uses; `interestSaved` is the exact figure already shown as `#adv-hero-value`) does against the user's *real* saved balance sheet instead of the hypothetical loan the panel otherwise models.
- **Empty state (no balance sheet saved)** is handled explicitly, never as a fabricated ₹0: the card (`#adv-worth-card`) always renders when a loan is entered, but shows `#adv-worth-empty` (a message plus a link to Dhanam Worth) instead of any figures until `worthSnapshot()` returns non-null.
- **Two calculations placed side by side, not one merged model.** The "Your Worth projection today" line is `calcNetWorthProjection()` called with `worthSnapshot()`'s own figures at the same +10-year horizon `renderWorthProjection()`'s hero already uses — bit-identical to what Dhanam Worth's own hero would show right now, by construction. The "With this plan's interest saved" line is that same figure **plus** `interestSaved` added on top as a flat number — deliberately *not* re-run through Worth's own generic total-liability decay a second time (Worth's `debtRate`/`debtYears` assumption is a single blended figure for *all* liabilities, not this specific home loan's actual rate/tenure) — and the on-screen caveat says so explicitly, per this app's "state, don't model" convention (R63) rather than silently reconciling two models that don't actually agree.
- **`.amounts-hidden`/"Hide amounts" does not reach here** — that mechanism is scoped to `#worth-main` only, and a real net worth figure rendered inside `hub-apartment` is outside it. **Decision: closed by default** (the collapse-card starts unopened, like `adv-compare-card` beside it) rather than extending the blur mechanism — the lighter, safer of the two options named in the Phase 16 brief, so a real balance sheet is never on screen unasked.
- **Persists nothing new.** Read-only against `DS` via `worthSnapshot()`; never writes `DS`, never resurrects a tier-2 assumption from storage beyond what `worthSnapshot()` already falls back to.

### Naming/ID conventions

DOM IDs are short prefixed codes tying markup to JS lookups via the `v(id)` (numeric value), `chk(id)` (checkbox), `set(id, txt)` (write text), and `el(id)` (raw element) helpers:
- `q-*` — Quick Estimate inputs (apartment)
- `d-*` — Detail panel inputs/outputs (apartment cost breakdown)
- `l-*` — Loan panel (home loan)
- `adv-*` — Advanced/prepayment loan comparison
- `sip-*` — SIP comparison within the loan panel
- `car-*` — company car lease inputs (`car-basic` is *total fixed pay*, `car-epf-amt` is the payslip EPF amount — the retired `car-epf-pct` is not coming back; see **Dhanam Car lease analysis specifics** above)
- `cc-*` — Compare Cars (ICE vs EV under lease) shortlist and assumptions. Shortlist rows follow `disb-tr-row`'s pattern (no per-field DOM ids; inline `oninput` closures index into the `ccCars` array by position) rather than individually-id'd fields; see **Dhanam Car — Compare Cars specifics** above.
- ~~`cb-*` — car buying/loan inputs~~ — **retired in Phase 14 (R59); do not reuse this prefix.** The section was absorbed into Compare Cars' per-car detail view; its ids are now `cc-*`. `grep -n 'id="cb-' index.html` returns nothing — the string survives only in comments explaining the migration.
- `sp-*` / `spt-*` — SIP planner (Dhanam Grow) tabs and inputs
- `disb-*` — loan disbursement / pre-EMI calculator
- `w-a-*` / `w-l-*` — Dhanam Worth asset / liability rows (generated from `W_ASSETS`/`W_LIABS`); other Worth elements are plain `w-*`

When adding a new field, follow the existing prefix for its section and wire it through the section's single `render*`/`calc*` function rather than adding a new update path.

### Core calculation functions

Each major feature has one `calc*`/`render*` entry point that reads all relevant inputs, computes, and writes all outputs for that section. Functions marked **(calc.js)** are pure (no DOM access) and live in `calc.js`, not the inline script — see **Testing** below.

- `calcDetail()` / `renderDetail()` — apartment cost breakdown (floor rise, parking, amenities, corpus, maintenance, legal, stamp duty, registration, GST)
- `calcEMI`, `loanAtYear`, `simulateLoan` **(calc.js)**, `renderLoans()` — home loan amortization and prepayment simulation. `calcEMI` takes an optional fourth argument `fv` (Phase 9, default `0`) — the balance still owed at term end, for a lease's residual/balloon buyout. At `fv = 0` it takes the exact original 3-argument code path, so every pre-existing caller listed here is bit-identical; only `calcOwnershipCost` (Compare Cars, lease mode) passes a non-zero `fv`.
- `renderAdvLoan()` — extra-EMI/lumpsum prepayment scenario comparison
- `calcSIP` **(calc.js)**, `renderSIPComparison()` — SIP-vs-prepayment comparison
- `calcIncomeTax`, `calcPerquisite` **(calc.js)**, `renderCarCalc()` — company car lease tax analysis (old vs. new regime). `calcPerquisite` holds the Income-tax Rules **2026** table (₹5,000 / ₹7,000 / +₹3,000 chauffeur, in force 2026-04-01) — see the Dhanam Car section above. `calcIncomeTax`'s new-regime branch applies **Section 87A marginal relief** (R34, Phase 8): above the ₹12L rebate threshold, tax *before cess* is capped at `taxable − 1200000`, which removes what used to be a cliff and stops binding at ₹12,70,588.24 (`1200000 + 60000/0.85`). The old-regime ₹5L rebate is a genuine cliff in law and must stay one — there's a test pinning it so a future "consistency" fix can't quietly extend relief to it.
- `calcCarDepreciation` **(calc.js)** — IRDAI depreciation-based resale estimate (20% year 1, 15%/yr thereafter). Was the standalone `renderCarLoan()`/`cb-*` section's engine through Phase 10-13; that section was retired in Phase 14 (R59) and its 5-year EMI hero, 3/5/7-year tenure grid, and resale-value/loan-balance chart were absorbed into Compare Cars' per-car detail view (`ccRenderLoanDetail()`, shown only in Loan mode). See **Dhanam Car — Compare Cars specifics** below.
- `calcRunningCost`, `calcInsuranceTotal`, `calcOwnershipCost`, `calcBreakevenKm`, `calcOwnershipCurve`, `calcLumpsumGrowth` **(calc.js)**, `renderCarCompare()` — Compare Cars (ICE vs EV cost of ownership under Loan/Lease/Cash), Phase 9, extended in Phase 13 (cost-vs-value chart + resale reveal) and Phase 14 (financing modes, absorbed car-loan detail, cross-mode comparison). `calcOwnershipCost` (renamed/generalized from Phase 9's `calcLeaseNetCost` in Phase 14/R55) is the shared engine across all three modes — see **Dhanam Car — financing modes** above and **Dhanam Car — Compare Cars specifics** below.
- `updateSIPPlanner`, `calcStepupSIP` **(calc.js)**, `updateStepupSIP`, `updateLumpsum` — Dhanam Grow SIP planner
- `renderLoanDisb()` — loan disbursement / pre-EMI tranche calculator (`section-disb` inside `hub-apartment`)
- `renderWorth()` — Dhanam Worth balance sheet, net-worth hero, change tile, and persistence (`hub-worth`); calls `renderWorthProjection()` and `renderWorthTrend()` as sub-steps, per the section's single-entry-point convention
- `calcNetWorthProjection` **(calc.js)**, `renderWorthProjection()` — the Worth hub's own net-worth projection (Phase 16/R65 extracted the formula into `calc.js`, no behavior change). `worthSnapshot()` + `renderAdvWorthBridge()` (`hub-apartment`'s `adv-section`) call the same function to show the same projection against the real balance sheet from inside the loan panel — see **Dhanam Worth specifics** above and **No backend; persistence** above for `worthSnapshot()`'s single-accessor rule.

### Testing

`calc.js`'s pure functions are covered by automated, dependency-free tests:
- `node tests.js` — runs in plain Node, zero installs. Exits 0 and prints a pass count when everything passes; exits non-zero with a failure list otherwise.
- `tests.html` — the same assertions rendered as a pass/fail page in a browser (open it like any other page, e.g. via `python3 -m http.server`), for whenever Node isn't handy.

Run both after touching anything in `calc.js`. They check exact values (e.g. zero-rate edge cases, IT-slab arithmetic, IRDAI depreciation-year-1), an independent bisection-based oracle for `calcEMI` (so a formula bug — not just a regression — would be caught), and a few monotonicity/conservation properties (e.g. `principalPaid + balance == P` at any point in a loan's tenure). They do **not** cover the DOM-coupled `render*`/`calcDetail`/`renderLoanDisb` functions in `index.html` — those are still verified by hand per the checklist below.

One known, deliberately-not-silently-fixed finding lives in these tests: `calcStepupSIP` (month-by-month simulation, ordinary-annuity timing) and `calcSIP` (closed-form, annuity-due timing) disagree by about 1% even at 0% step-up, because they use different interest-crediting conventions. The test documents and bounds this gap rather than asserting the two are equal — see the comment above that test in `tests.js` before changing either function.

### Excel export

Exports are hand-built with no library: `buildZip`/`_u16`/`_u32` construct a raw ZIP, `buildExcel`/`rowsToSheetXml` generate minimal SheetXML, and `exportDetailExcel`/`exportLoanExcel`/`exportCombinedExcel`/`exportSnapshot`/`exportWorthExcel` assemble the rows per report. If you need to add a new export, follow the `buildDetailRows()`/`buildLoanRows()`/`buildWorthRows()` pattern (return an array of row arrays) and pass it into `buildExcel`.

### Charts

One dependency-free inline-SVG line/area chart helper is shared by every chart in the app rather than each hub rolling its own. **Eight call sites as of Phase 14**: the Worth trend chart (`w-trend-chart`), the principal-vs-interest chart in the loan panel (`l-pvi-chart`), the SIP corpus-growth curve on all three Dhanam Grow tabs (`sp-growth-chart`, `su-growth-chart`, `ls-growth-chart`), and three in Compare Cars — the cost-vs-value curve (`cc-owncurve-chart`, R51), the absorbed resale/loan-balance chart (`cc-depr-chart`, formerly `cb-depr-chart` before Phase 14's R59 migration), and the net-cost-vs-annual-km crossover (`cc-chart`, R42).

- **`renderChart(targetId, series, opts)` is the entry point — always call this, not `chartSvg()` directly.** It measures the host element and draws at a 1:1 scale. This matters: the SVG uses `preserveAspectRatio="none"` so it always fills its container (never causing horizontal scroll at 375px), which means a viewBox that *doesn't* match the container's real pixel width gets non-uniformly stretched — dots render as visible ellipses and stroke width varies by line direction. Measuring is what keeps that from happening.
- `chartSvg(series, opts)` takes one or more `{ values, color, area }` series on a shared scale and returns the `<svg>` string. It degenerates gracefully: a single point renders as one dot (no path), and series longer than 24 points draw only the final point's dot to stay legible.
- **Redraw contract (R11, Phase 3b):** `renderChart()` caches the last `(series, opts)` it was called with per `targetId` in `chartCache`, and attaches one `ResizeObserver` per host (`chartObservers`) the first time that host is rendered. Whenever the host's content-box size changes away from 0 — a collapsed card opening, a hub or SIP-planner tab becoming active, a window resize/orientation change, anything — the observer redraws from the cached series at the host's *current* width. This is the **only** redraw mechanism in the app; there is no per-caller `redraw()` convention and no manual resize listener. A hidden host still measures 0 on the very first render and draws once against a 600px fallback (visibly distorted if you inspect it mid-hidden), but nothing depends on that frame being correct — the observer fixes it the moment the host becomes visible.
  - **The observer is keyed by DOM node, not just by id.** If a host's *parent* rewrites its `innerHTML` (e.g. `cc-depr-body` rebuilds its whole subtree on every `ccRenderLoanDetail()` call — originally `cb-depr-body`/`renderCarLoan()`, the same pattern before Phase 14's `cb-*` migration), the old host node is detached and a new one with the same id takes its place. `renderChart()` detects this (`existing.node !== host`) and re-creates the observer on the new node — without that check, the detached node's observer would silently stop firing forever. If you add a chart whose host lives inside a container that gets `innerHTML`-rewritten on every render, this is why it still works; don't remove the check as a "simplification."
  - **`toggleChartCard(cardId, redraw)`** is now a thin wrapper: it toggles the card, and optionally calls `redraw()` once more if the card just opened (useful when the chart's data hasn't been computed yet at all, not just hidden). The Worth trend card is the only caller (`renderWorthTrend` is actually already computed on every `renderWorth()`, open or not, so the `redraw` arg here is a belt-and-braces immediate correction — the ResizeObserver would catch it a tick later regardless).
- **Never write a chart host's `innerHTML` directly — use `clearChart(targetId)` to empty one (R15, Phase 3c).** `chartCache` is never pruned by `renderChart()`, so a bare `el(id).innerHTML = ''` leaves the old series cached; the next time that host's size changes away from 0 (card expands, window resizes), the `ResizeObserver` redraws the stale data right back onto the screen — a real bug found in `renderWorthTrend()`'s two paths that bypassed `renderChart()` (zero-history and one-history-point). `clearChart()` empties the host **and** deletes the cache entry, so the observer's next callback is a no-op. Any future "hide/blank this chart because its data is gone" case must go through it.
- **`chartSvg()` spaces points by array index, not by their actual value (R17, Phase 3c).** A caller that plots a non-uniform sequence — e.g. years `[1,2,3,5,7]` — draws the 5→7 gap the same width as the 1→2 gap, silently flattening whatever shape depends on the true spacing. Callers with non-uniform steps must expand to a uniform sequence before calling `renderChart()` (the car-depreciation curve plots every year 1–7, not just the five years the table shows); `chartSvg` itself stays index-based since every other caller's steps already are uniform and widening the shared helper for one caller isn't worth it.
- **The y-axis is auto-scaled to the data and there are no gridlines or axis labels**, so a 1% wiggle fills the chart exactly like a doubling would. Any chart whose values aren't already readable from an adjacent table or hero figure must print its own value range — the Worth trend chart does this in its caption, since its historical values appear nowhere else. **Separately, every chart now states its x-axis extent too** (R19, Phase 3b/3c) — `.chart-caption`'s `Year 1 … Year N` on all five year-based charts (the resale curve fixes it at `Year 1 … Year 7`, matching its uniform 1–7 plot from R17) — since there are no axis labels to read the span from otherwise.
- If you add a chart with an amount-bearing legend or caption, remember the `.amounts-hidden` blur rule above — mark those elements `w-amt`.
- Chart colours are drawn from the existing neutral scale, not a dedicated "chart palette" — there is no `--text-faint` token in this codebase (despite an earlier draft of this task brief assuming one); a third non-accent series uses `--text-mid` (secondary) alongside `--text-dim` (most muted), per palette rule 1 (no new hues without revisiting `COLOR-PALETTE-ANALYSIS.md`).
- **A chart host must be a stable DOM node, not markup regenerated inside a parent's `innerHTML` template (R20, Phase 3c).** `renderChart()`'s node-identity check (above) papers over a host that gets recreated on every render, but it still tears down and rebuilds a `ResizeObserver` every time — on `cb-depr-chart`, that used to mean every keystroke in the car-buying panel. `cb-depr-chart` is now a static sibling of `cb-depr-body` in the markup (only `cb-depr-body`'s own table/note gets rewritten); prefer that shape for any new chart whose surrounding content re-renders often. Keep the node-identity check regardless — it's still what protects the cases where a stable host isn't practical.
- **`calcStepupSIP` and `calcSIP` disagree by ~1% even at 0% step-up (B6, still open — see Testing below) — do not plot them against each other as two lines on one chart.** The step-up tab's growth chart and its `su-vs-flat` figure both compare `calcStepupSIP(monthly, stepup, cagr, y)` against `calcStepupSIP(monthly, 0, cagr, y)` — the *same* function at 0% step-up — specifically so the two lines are exactly equal at year 1 (a step-up SIP is definitionally identical to a flat one until its first step, at the start of year 2) and never invert. Comparing across the two functions instead reintroduces the R16 bug: at this tab's own default settings it drew the step-up line **starting below** the flat line.

**Wide tables use the same overflow discipline as charts (`.table-scroll`, R10).** A `.table-scroll { overflow-x: auto; }` wrapper `<div>` sits around every `<table>` that could plausibly overflow a 375px viewport — the Detail panel's Payment Schedule, the Loan Disbursement schedule, all three Dhanam Grow milestone tables, and the absorbed car depreciation table (`#cc-depr-body`, wrapped inside its own `innerHTML` template string, since that table is rebuilt on every `ccRenderLoanDetail()` call — originally `cb-depr-body`/`renderCarLoan()` before Phase 14). There are **6** such wrappers as of Phase 15 — the six the sentence above names, one each. (This line said "7" from Phase 14 until Phase 15's review caught it: a bare `grep -c table-scroll index.html` returns 7 because it also counts the `.table-scroll` **class definition** itself. Count `grep -c 'class="table-scroll"'` instead.) Reuse this class for any new table rather than adding a one-off wrapper.

### Manual test checklist — landing page, loan disbursement & persistence

No DOM-level test framework exists (see **Testing** above for the automated coverage that does exist, scoped to `calc.js`); re-run this list by hand (`python3 -m http.server`) whenever touching `hub-landing`, `section-disb`, `hub-worth`, the persistence layer, or navigation:

1. **Landing default**: fresh load shows the landing page, not the apartment hub; nav shows `⌂ Home` as the active tab.
2. **Tile navigation**: each tile, including Dhanam Worth, opens its destination and marks the matching nav tab active. Every tile's `.tile-tool` subline should name a destination that actually exists (no orphaned or renamed hubs/sections).
3. **Loan deep-link**: the "Finance My Home" tile (`openLoanCalc()`) lands on the apartment hub with `section-loan` expanded and scrolled into view, with results already rendered; clicking it a second time (panel already open) must not collapse the panel. If `section-detail` or `section-disb` was already open, it collapses (accordion, item 24) and the loan panel opens in its place without a visible upward jump before the scroll.
4. **Disbursement deep-link**: the "Buying Under Construction?" tile (`openDisbCalc()`) lands on the apartment hub with `section-disb` expanded and scrolled into view; clicking it a second time must not collapse the panel. The "Loan Disbursement (Pre-EMI)" action button inside the apartment hub opens the same panel. Same sibling-collapse expectation as item 3 if another panel was already open.
5. **Return paths**: the `⌂ Home` tab and the header logo both return to the landing page from any hub.
6. **Hover states**: tiles lift and show the gold border on hover (desktop); no hover artifacts on touch/mobile widths.
7. **Responsive**: at ~375px width the tile grid is a single column with no horizontal scroll; inputs render at ≥16px on touch widths (no iOS zoom-on-focus).
8. **Existing hubs unaffected**: apartment quick estimate, car lease, car loan, and all three SIP planner tabs still recalculate live on input.
9. **Disbursement math spot-checks**:
   - Single tranche of 100% at month 0 must match the plain home-loan EMI panel for the same amount/rate/tenure (total interest equal, no pre-EMI phase).
   - Two tranches (e.g. 50% at month 0, 50% at month 12, 9% rate): pre-EMI interest for the first 12 months must equal `0.5 × loan × 0.09` (interest on only the disbursed half).
   - Adding/removing a tranche row recalculates immediately; tranche percentages that don't sum to 100% show a visible warning rather than silently computing.
   - Zero/empty inputs must not produce `NaN` or `Infinity` in any output cell.
   - Typing multiple digits into a tranche's `%`/month input must not lose keyboard focus between keystrokes.
10. **PWA cache**: after bumping the `sw.js` cache version, a previously installed instance picks up the new landing page on reload.
11. **Persistence — failure paths first** (these matter more than the happy path, because a bad blob is the only failure a reload can't rescue the user from). In devtools → Application → Local Storage:
    - Write garbage into `dhanam.v1` and reload: the app must load normally from defaults, and the Worth hub must say the saved data couldn't be read.
    - Set `v` to a different number and reload: the blob is ignored cleanly, same as above.
    - Delete `dhanam.v1` but leave `dhanam.seen` and reload: the Worth hub reports that the browser cleared the data and offers restore.
    - Override `localStorage.setItem` to throw, then type: calculators keep working and the UI says it couldn't save — it must never still claim "Saved on this device".
    - Private/incognito window: everything calculates; no crash.
12. **Persistence — happy path**: enter a balance sheet, reload, values and net worth return. Turn the loan panel's "Remember my inputs" toggle on, reload, inputs return and the total's hint reads "Remembered on this device"; toggle off and confirm `dhanam.v1` is gone.
13. **Tier-2 leakage check**: confirm the stored blob contains no rates, stamp duty, tax-slab, or depreciation values. An untouched `l-rate` (8.75%) must **not** be stored; change it to 8.4% and it must be.
14. **Change tile**: no tile at all on a first-ever visit; after planting an older `history` entry, editing one balance up shows a green ▲ with "Up ₹X (+Y%) since <date>"; editing down shows a red ▼ with "Down"; returning to the exact prior figure shows a neutral "No change". Readable in greyscale (colour is never the only signal).
15. **Backup round-trip**: Download backup → Erase my data (form clears, `dhanam.*` keys gone) → Restore backup returns the same balance sheet and history. A non-JSON file and a wrong-version backup are both rejected with a message, not a crash.
16. **Hide amounts**: every figure blurs — hero, assets/liabilities totals, per-row inputs, per-row `%` shares, and the change tile's "was ₹X" line. Labels stay readable.
17. **Worth empty/edge states**: an all-empty sheet shows `—` everywhere and writes nothing to storage; liabilities exceeding assets shows a red `−₹X` hero; no `NaN`/`Infinity`/`undefined` anywhere in the hub. **Also (R18, Phase 3c):** clearing `ls-amount` on the lumpsum tab must not put `NaN×` in the milestone table's Multiplier column (it degrades to `—`, matching `ls-mult`'s existing guard); sweep for the same unguarded-division shape in any new table.
18. **Worth projection bridge**: hidden with an all-empty sheet; entering any balance shows a "Projected Net Worth" card with a +10-year hero and a +5/+10/+20-year row; changing the monthly-savings/CAGR/debt-rate/years inputs recalculates live; a debt-free-years of 1 and a fully-paid-off (zero liability) sheet both compute without `NaN`/`Infinity`; a projection that goes negative renders with a `−` prefix and red.
19. **Worth trend chart**: absent with zero history; a friendly "not enough history yet" message with exactly one history point; a real line/area chart with two or more points; stays closed by default and opens via its collapse header; blurred by "Hide amounts" like every other figure. **Also (R15, Phase 3c):** build 3+ history points, "Erase my data", then plant exactly one balance — expanding the trend card (or resizing the window) in that state must show the "not enough history yet" message, never a repaint of the erased history. `chartCache.has('w-trend-chart')` should be `false` at that point.
20. **Worth Excel export**: `exportWorthExcel()` is a no-op with a toast on an empty sheet, otherwise downloads a workbook with assets, liabilities, net worth, and history sheets/sections that open cleanly.
21. **Hero-answer-first density (loan panel)**: opening `section-loan` shows a 20-year EMI hero and a principal-vs-interest chart with no interaction; the 15/20/30-year comparison and custom-year grid are reachable only after expanding the "Compare" collapse card, which starts closed. Opening "Advanced: Extra Payments" and "Buy vs. SIP" each show their own hero answer before their comparison grids, which also start collapsed. All three heroes and their nested collapse cards hide cleanly (not just visually empty) when no loan amount is entered yet — **and with the property cost at 0 the panel still shows the "Enter total property cost above" guidance** rather than a blank gap (that message lives in `#l-empty`, not in the now-collapsed card).
22. **Charts don't overflow or distort**: the trend chart, all three Dhanam Grow tabs' corpus-growth charts (monthly/step-up/lumpsum), the principal-vs-interest chart, and the car-buying resale-value curve all render at 375px width with no horizontal page scroll, and stay legible at both 1 data point and their maximum (120 history points; a 50-year SIP). Check the **dots are circular, not oval** — an ellipse means a chart was drawn against a mismeasured (usually hidden) container. Expanding the Worth trend card must redraw it, not just reveal a stale 600px-viewBox render. The step-up chart's year-N corpus matches `su-corpus`/the milestone table; the lumpsum curve's year-N value matches `ls-corpus`; the resale curve's year-N value matches the depreciation table row for year N, including with a 100%-down-payment (zero-loan) car. **Also (R16/R17/R19, Phase 3c):** the step-up chart's gold and grey lines are pixel-identical at year 1 and at an explicitly-typed `0` in the Step-up % field — not just the field left empty — must actually zero out the gap (`su-stepup`'s old `|| 10` fallback treated a real `0` the same as blank; fixed alongside R16); the resale curve has 7 points, not 5, and its visible slope flattens from year 1 to year 7 rather than the 5→7 segment reading as steep as 1→2; all five year-based charts (`l-pvi-chart`, `sp-growth-chart`, `su-growth-chart`, `ls-growth-chart`, and the resale curve — **`cc-depr-chart` since Phase 14's R59 migration, `cb-depr-chart` before it**) show a `Year 1 … Year N` (or `Year 7`) caption.
23. **Projection discloses its cash-flow assumption**: with any liability entered, the projection card states the implied monthly repayment ("assumes about ₹X/mo of repayments … paid from income, which this balance sheet doesn't track"); the figure matches `calcEMI(liabilities, debtRate, debtYears)`, updates with those inputs, disappears when liabilities are 0, and blurs under "Hide amounts".
24. **Dhanam Home accordion (R14)**: opening any of `section-detail`/`section-loan`/`section-disb` collapses whichever of the other two was open, leaving exactly one `action-btn` `.active`; clicking an open panel's own button still collapses it; values typed into one panel survive switching to another and back (collapsing never clears inputs or re-runs a panel's first-open prefill); the combined-export buttons, once shown, stay visible regardless of which single panel is currently open. Does **not** apply to the nested compare/advanced/SIP `.collapse-card`s inside `section-loan` — those can be opened independently of the accordion.
25. **Chart redraw on every reveal path (R11)**: dots stay circular (not oval) after — switching Dhanam Grow tabs in any order; leaving the Grow hub with a non-monthly tab active and re-entering it; collapsing `section-loan` via the accordion, hitting Reset, and reopening it; resizing the browser window from ~1200px down to 375px and back. No chart is ever left showing a distorted render once its host is visible on screen.
26. **Grow-hub years inputs are clamped, not just defaulted (R18, Phase 3c)**: typing a value above 50 into `sp-years`/`su-years`/`ls-years` snaps the field itself to 50 (not just the calculation) and the tab stays responsive — this matters most on the step-up tab, whose chart calls the month-by-month `calcStepupSIP` once per year and would otherwise do ~n² work per keystroke for a large typed value. Typing below 1 snaps to 1. Clearing the field falls back to 20, matching the pre-3c default behaviour.
27. **Dhanam Car EPF is an input, not a derivation (R33, Phase 8)**: all three scenario cards show the **same** "EPF deduction" figure, and it equals whatever is typed into `car-epf-amt` — changing `car-basic` must move Annual Gross, Taxable Income and tax but leave the EPF line untouched. Take-home reads exactly `annual gross ÷ 12 − EPF − monthly tax` on every card, so it can be reconciled against a real payslip line by line. Clearing `car-epf-amt`, typing a negative, and typing an EPF larger than gross must all compute without `NaN`/`Infinity` (a large EPF may legitimately drive take-home negative). Switching to the old regime still caps the 80C deduction at ₹1.5L using the same annualized figure.
28. **Perquisite figures are current law and stated on screen (R37, Phase 8b)**: the scenario summary's "Perquisite value (personal use)" reads ₹5,000/mo (₹60,000/yr) with both boxes unchecked, ₹7,000 with "Engine > 1600cc", and +₹3,000 with "Company Driver Provided" — never the old ₹1,800/₹2,400/₹900. Ticking either box must move Carve-out A's and Additive B's Taxable Income and tax while leaving **Baseline's completely unchanged**. The caveat list states the Income-tax Rules, 2026 and their 1 Apr 2026 commencement, and the engine checkbox says "(leave off for electric)".
29. **New-regime 87A cliff is gone (R34, Phase 8)**: in the car hub on the new regime, set inputs so taxable income lands just above ₹12,00,000 (e.g. `car-basic` ≈ ₹1,06,300, no bonus) — Annual Tax + Cess must be a few hundred rupees, not ~₹62,400, and nudging salary up must never reduce take-home anywhere across the ₹12L–₹12.7L band. `node tests.js` covers the arithmetic; this item is the on-screen confirmation.
30. **Compare Cars blank/partial rows contribute nothing (R39/R44, Phase 9)**: a row with only a price, only a mileage/efficiency figure, or neither, must not appear in the hero, the ranked result cards, the breakeven rows, or the crossover chart. Adding a second, third, fourth, and fifth car all recompute immediately; the "Add car" button stops at 5.
31. **Compare Cars typing never loses focus (D6 trap, Phase 9)**: continuously typing multiple digits into a shortlist row's price or mileage/efficiency field must not drop keyboard focus between keystrokes — the same check `section-disb`'s tranche rows already require. Typing into the Assumptions card's `cc-city-km`/`cc-hwy-km` fields must not rebuild the shortlist rows either.
32. **Compare Cars balloon EMI matches the plain-loan invariant (R38, Phase 9)**: with one car entered, a 0% residual must produce the same EMI as `calcEMI` with no `fv` argument at the same price/rate/tenure — the direct analogue of the existing single-tranche-at-month-0 ≡ plain-EMI check. A 100% residual computes without `NaN`/`Infinity` (EMI approaches the interest-only payment).
33. **Compare Cars breakeven branches render as sentences (R41, Phase 9)**: an EV that costs more per km than the reference ICE car never shows a breakeven figure — it says it never breaks even. An EV whose fixed cost already undercuts the reference car says "Already cheaper today" and shows no negative km figure. Otherwise it says "Needs N more km/yr". With one ICE car and no EVs, or no ICE car at all, the breakeven card hides entirely rather than half-rendering.
34. **Compare Cars crossover chart is undistorted (R42, Phase 9)**: dots stay circular (not oval) at 375px width and after every reveal path — opening the collapse card the first time, switching away from and back to `hub-car`, and resizing the window — per the shared `renderChart()`/`ResizeObserver` contract (R11).
35. **Compare Cars tier-2 leakage check (R43, Phase 9)**: after entering a shortlist, inspect `dhanam.v1` in devtools — `carCompare` must contain only `cars` (each car's own type/name/price/efficiency/engine flag) and `cityKm`/`hwyKm`. No petrol price, charging rate, lease rate, marginal tax rate, maintenance, insurance rate, or IDV depreciation figure may appear anywhere in the stored blob. Erase-and-restore round-trips the shortlist like any other tier-1 data.
36. **Compare Cars rank-1 is gold, no green in the section (R39/R40, Phase 9)**: the cheapest car's result card (by net cost **before** resale credit, per B12) carries the gold border/badge; every other card is neutral. The resale-credit line and the "Already cheaper today" breakeven verdict are the only two green usages in the section, and both are real positive deltas, not decoration.
37. **Lease Tax Analysis hero density (R45, Phase 10a)**: opening `hub-car` shows a single `.total-card` hero above the scenario grid — whichever of Baseline/Carve-out A/Additive B currently wins, its effective net/mo, an in-words advantage sentence ("X saves you ₹Y/mo over owning the car yourself", or the baseline-wins sentence when neither lease structure helps), and the perquisite value/mo directly under it — with the first screenful of the section (below the input panel-card) showing **≤ ~10 numbers total**. Changing `car-basic`, `car-emi`/`car-fuel`/`car-driver-allow`, either checkbox, or the regime radio must move the hero's label/value/note live, with no stale state. "Compare all three scenarios ▾" starts closed and, once expanded, still shows all three cards unchanged; every number the old `car-summary-box` used to state (perquisite, each scenario's effective net, the best-option verdict) is reachable on the hero or a card — none deleted, none stated a third time.
38. **Car Buying loan-analysis hero density (R46, Phase 10b)**: with a price/rate entered, a `.total-card` hero shows the **5-year** EMI (not 20) with total interest and total paid, above a closed-by-default "Compare tenures ▾" holding the 3/5/7-year grid; the first screenful (below the input row) shows ≤ ~10 numbers. A 0% down payment (loan = 0) or a 0% interest rate hides both the hero and the compare card exactly like the grid used to hide itself — no `NaN`/`Infinity`, and the 5-year card inside the expanded grid matches the hero's EMI/interest/total exactly (same `tenureStats()` call). The depreciation table/chart below (`cb-depreciation`) is unaffected — same 5-row table, same 7-year chart, same `Year 1 … Year 7` caption, still driven off `price` alone.
39. **Compare Cars per-card disclosure (R47, Phase 10c)**: with 3+ cars entered, the first screenful below the hero/breakeven/chart shows rank-1's card **open** (full breakdown visible: resale credit, net cost after resale, raw cash outflow, signed tax saved) and every other rank's card **collapsed** to just its badge/name/net-cost/`EMI · ₹/km` line. Clicking "Full breakdown ▾" on a rank-2+ card opens only that card — rank-1's and every other card's open/closed state is untouched (independent disclosure, not an accordion). Collapsing rank-1's own card and then changing an unrelated field (e.g. petrol price, or another row's price) must leave it collapsed and must not touch any other card's state — the per-card choice, not just a redraw default, survives the re-render. Removing a car resets every card back to the rank-1-open default (the index-to-car mapping shifted, so a stale toggle would otherwise point at the wrong car after the splice).
40. **State selector changes stamp duty/registration everywhere it's shown (R21, Phase 6a)**: switching `q-state` moves `q-reg`, `d-stamp`/`d-regfee` (if Detail is open), Quick Estimate's total, and every "as of `<state>`" label live; Telangana reproduces today's numbers exactly; all three rate fields stay manually editable after a state is picked; opening Detailed Cost Analysis for the first time after picking a non-Telangana state prefills that state's split rates, not Telangana's; the exported Excel/PNG snapshot's title and notes name the selected state, not a hardcoded Hyderabad/Telangana; `dhanam.v1`'s `propertyState` key holds only a state code, never a rate; "Reset Home Inputs" returns the selector to Telangana and matches a fresh load.
41. **The car-loan EMI runs through `calcEMI` (R10)** — *historical: this item was written against `renderCarLoan()`, which Phase 14/R59 retired. The invariant survived the migration and now applies to `ccRenderLoanDetail()`*: the tenure grid's cards still agree bit-for-bit with each other (same `tenureStats()` call, carried across unmodified) and with the plain-EMI formula for the same principal/rate/tenure — this was a refactor, not a behavior change, so there is nothing new to see, only nothing to have broken. Verified in practice by item 52's number-for-number parity check.
42. **About page reachable, provenance-complete, and advice-free (R22, Phase 6b)**: the header's "ⓘ About" link works from every hub without also triggering the logo's own "back to landing" handler, and doesn't visually collide with the header title at 375px; the landing footer's "About · How this works" and "Have feedback?" links both work, the latter as a plain `mailto:`; every numeric assumption in the app (perquisite, both tax regimes, IRDAI depreciation, state stamp duty/registration, GST, the four rate defaults, Worth's projection defaults, Compare Cars' tier-2 group) appears in the provenance list with a date or "as of" note; the privacy claim holds under an actual devtools Network-tab check (zero off-origin requests, on this page and everywhere else); the build stamp shows a real date, not a placeholder; no sentence anywhere on the page reads as a recommendation ("you should," "we recommend," "the best option is").
43. **Prepayment tile deep-links past the outer panel (R24, Phase 6d)**: the "Should I Prepay My Loan?" tile opens `hub-apartment`, expands `section-loan`, AND expands the nested `adv-section` ("Advanced: Extra Payments Projection"), scrolling `adv-section` itself into view rather than just the outer panel; a second click on the tile leaves `adv-section` open rather than collapsing it. Opening "Buy vs. SIP: What if you invested instead?" shows the `.sip-caveat` disclosure immediately above the hero, readable without expanding the closed-by-default compare card, naming both the SIP side's uncertainty/tax exposure and the loan side's own ignored Section 24/80C benefits — with no specific tax rate printed anywhere in it.
44. **Nav scroll-shadow doesn't block taps (R10)**: at a narrow width where the 5 nav tabs overflow, a visible fade hints there's more to scroll; scrolling the tab row all the way to its end and tapping the last tab (Dhanam Worth) still activates that hub — the fade is a background-image effect with no element sitting on top of the tabs.
45. **Compare Cars resale figure stays hidden until revealed (R50/R52, Phase 13)**: with a shortlist entered, no resale/after-resale figure appears anywhere on the hero or any result card by default; the `.sip-caveat`-styled disclosure (`#cc-caveat`) is visible immediately above `#cc-hero`, readable without expanding or clicking anything, and states in words that the ranking counts cash out the door and ignores what the car is worth afterwards.
46. **Cost-vs-value chart agrees with the card it describes (R49/R51, Phase 13)**: for the car selected in "Cumulative Cost vs. Car Value", the gold cumulative-cost line's final point equals that car's Net Cost shown on its result card; clicking "Reveal resale-adjusted net cost" shows a figure equal to the on-screen gap between the two lines at the final year (cumulative cost minus car value) — not a second, independently-computed number.
47. **Cost-vs-value chart host is stable under typing and resizing (R51, Phase 13)**: continuously typing multiple digits into any shortlist row's price or mileage/efficiency field, or any Assumptions-card field, must not drop keyboard focus in that field and must not visibly rebuild the chart region (the D6/R20 trap); the chart's dots stay circular (not oval) at 375px width after every reveal path — opening `hub-car` for the first time, switching away and back, resizing the window — per the shared `renderChart()`/`ResizeObserver` contract (R11).
48. **Resale reveal is tier-3, not tier-1 (R52, Phase 13)**: click "Reveal resale-adjusted net cost", reload the page — the reveal is closed again (never remembered); inspect `dhanam.v1` in devtools and confirm no key related to the reveal's open/closed state, or the selected car in the cost-vs-value chart, appears anywhere in the stored blob (only `ccCars` and the two driving-pattern fields belong there, per item 35).
49. **Compare Cars caveat list is honest about what the resale figure is (R54, Phase 13)**: the "Important Notes & Caveats" panel states that the resale/depreciation figure comes from an IRDAI insurance (insured-declared-value) schedule, not a market resale prediction; that it's one ICE-shaped curve applied to EVs too; and that the reveal is a forecast while the ranking above it is not.
50. **Car Buying depreciation chart's loan-balance overlay behaves correctly (R53, Phase 13, answers B8)**: with a price and loan amount entered, `cb-depreciation`'s chart shows a second, red line for the outstanding loan balance, plotted only across its own 5-year tenure (not the chart's full 7-year window — a loan sampled past its own tenure would clamp to a flat zero-balance line for the remaining years, which must not appear), alongside the existing gold car-value line (now without its area fill, so the crossing isn't obscured — this is a real visual change from pre-Phase-13, not "exactly as before"), with a legend distinguishing them; a 100%-down-payment car (loan = 0) shows the depreciation curve with **no** balance series and no flat zero line; if the loan balance ever exceeds the car's value within its 5-year window, the note below the chart names the actual first-through-last underwater year range in words (never "from year N onward", since underwater is structurally a leading interval that can end once depreciation slows) — otherwise it says the balance never exceeds the car's value. **Superseded by items 52-53 below** — this exact `cb-*` section was retired in Phase 14/R59 and its behavior re-homed into Compare Cars' per-car detail view; kept here as the historical record of what shipped in Phase 13.
51. **Mode selector gates fields on/off with no bleed between modes (R56-R58, Phase 14)**: a fresh load of `hub-car` defaults to Loan mode (`#car-mode-btn-loan` carries `.active`, `#car-lease-panel`'s `display` is `none`) — no salary/bonus/EPF/regime field is visible anywhere on screen. Clicking Lease reveals `#car-lease-panel` with its hero already computed live; clicking back to Loan hides it again without clearing anything typed into it. The Assumptions card's `#cc-field-residual`/`#cc-field-marginal`/`#cc-field-driver` show only in Lease mode, `#cc-field-cash-cagr` shows only in Cash mode, and each shortlist row's Down Payment field (inside `ccRowHtml()`) renders only in Loan mode — switching modes back and forth toggles visibility only via `updateCCModeFields()`/`ccRowHtml()`, and never clears `ccCars`, the lease panel's own salary inputs, or any per-car down payment already typed. `#cc-rate-label`/`#cc-years-label` text changes with the mode ("Loan Interest Rate"/"Loan Term" vs. "Lease Interest Rate"/"Lease Tenure" vs. "Interest Rate"/"Ownership Term") so which mode is active is legible from the label text alone, not just from which fields disappeared.
52. **Absorbed car-loan detail view matches the retired `cb-*` section number-for-number (R59, Phase 14)**: in Loan mode, with a shortlist car's price/down payment/the shared Assumptions-card rate entered, the per-car detail view's "Compare 3 / 5 / 7-year loan tenures" card (`#cc-tenure-card`, closed by default) and "Estimated Resale Value (IRDAI Depreciation)" section (`#cc-depr-section`) show the identical EMI/total-paid/total-interest figures (same `tenureStats()` call, carried across unmodified) and the identical 5-row depreciation table / `Year 1 … Year 7` chart that the pre-Phase-14 `cb-*` section produced for the same inputs. Both hide entirely in Lease or Cash mode (no loan-tenure or depreciation-table concept applies there) — except the depreciation table/chart itself, which still renders off `price` alone in every mode (R13's original rule, unchanged). `grep -n 'id="cb-' index.html` returns nothing — no live `cb-*` DOM id survives anywhere in the page; only historical comments mention the string.
53. **Loan-balance overlay tracks the actual selected loan term, not a fixed tenure (R53's fixes carried into R59, then re-fixed post-Phase-14-review)**: in Loan mode with a loan amount (price minus down payment) greater than 0, `#cc-depr-chart` shows a second red loan-balance line sliced to `cc-years`' own selected term (3/4/5yr) only — it must never extend past that term as a flat post-payoff zero-balance line, and switching `cc-years` between 3/4/5 must move both the balance line's length and the underwater note's stated year range live. A 100%-down-payment car (loan = 0) shows the depreciation curve with no balance series at all. The note beneath the chart names the actual first-through-last underwater year range in words when the balance exceeds the car's value at any point within that term, and otherwise says it never does — never "from year N onward" (same wording rule item 50 already required of the retired `cb-depr-chart`, now sourced from `ccRenderLoanDetail()`). Switching to Lease or Cash mode hides the entire depreciation section's loan-balance series (no loan concept in those modes) while the resale-value line itself keeps rendering. **Regression note:** the first Phase 14 build pass carried R53's literal `5`-year constant across instead of reading `cc-years` — correct only by coincidence at the section's 4-year default being close to 5, and wrong (a false "underwater" claim, or a balance line running past a real 3-year payoff) at every other term. Caught in code review; fixed before this phase shipped.
54. **Opportunity-cost reveal is tier-3, matching Phase 13's resale-reveal pattern exactly, and shown identically in every mode (R58/B13, Phase 14; visibility fixed post-review)**: "Reveal opportunity cost of tied-up capital" (`#cc-opp-reveal-btn`) is visible and closed on every fresh load and every re-entry into `hub-car`, **in all three modes, not just Cash** — the first build pass gated it to Cash-only, which violated B13's own fairness rule by never showing a Loan-mode user what their down payment's opportunity cost was; fixed in review. Clicking it shows a figure — `calcLumpsumGrowth(capital, cagr, years) − capital` via `ccOpportunityCost(carMode, ...)`, where `capital` is the full price in Cash, the down payment only in Loan, and ~₹0 in Lease (a lease ties up no capital upfront — a real structural fact, not an omission) — behind a permanent, adjacent, mode-aware text label ("What this cash / this car's down payment / the capital tied up... could have earned if invested instead"), never a bare styled number. Reload the page after revealing it: the reveal is closed again, never remembered. Inspect `dhanam.v1` in devtools: no key for `ccOppRevealOpen`, `ccCrossRevealOpen`, `ccRevealOpen`, `ccOwnCurveIdx`, or `carMode` appears anywhere in the stored blob — only `ccCars` and the two driving-pattern fields belong there (per item 35), unchanged by Phase 14's additions.
55. **Cross-mode comparison shows one car, three genuinely comparable columns, with its hidden inputs stated in words (R60, Phase 14; transparency note added post-review)**: with any car selected in "Car Detail — Cost Over Time," the "Lease vs. Loan vs. Cash — this car, three ways" card (`#cc-crossmode-card`, open by default) below it shows three cards — Lease, Loan, Cash — computed for that *same* car using its own down payment (loan column) and the shared Assumptions-card rate/term/marginal-rate/driver inputs for all three, regardless of which mode currently drives the ranking above; the column matching the page's active `carMode` is visually marked "(current mode)". Because the Assumptions card only ever shows ONE mode's field group (`updateCCModeFields()`), the Lease column's residual/marginal-rate/driver and the Cash column's CAGR are invisible on screen outside their own mode — found in review as a real risk (Lease could read as unconditionally cheaper via an invisible marginal tax rate a Loan/Cash-mode visitor never sees), fixed by a permanent `#cc-crossmode-assumptions` line above the cards stating the exact residual %, marginal rate, driver flag, and CAGR in use, live, in every mode. "Reveal resale credit & opportunity cost for all three" is closed by default and, once opened, applies both figures identically to all three columns in the same click — never a friendlier default for one mode's column than another's (B13's fairness rule made visible). The resale credit shown is the same `calcCarDepreciation(price, years)` figure in every column, since `resaleValue` never depends on `mode`.
56. **Absorbed chart hosts and result cards stay stable under typing and mode switching (R20, extended to Phase 14's new UI, Phase 14)**: continuously typing multiple digits into any shortlist row's price/mileage/down-payment field, or any Assumptions-card field, must not drop keyboard focus and must not visibly rebuild `#cc-owncurve-chart`, `#cc-depr-chart`, `#cc-tenure-cards`, or `#cc-crossmode-cards` — all four sit as static siblings outside `#cc-results`' innerHTML template, which is what gets rewritten on those keystrokes instead. Both chart hosts' dots stay circular (not oval) at 375px width after every reveal path: opening `hub-car` for the first time (Loan mode, the default), switching to Lease then back to Loan, switching to Cash, resizing the browser window, and navigating away to another hub and back.
57. **No input/select/textarea renders below 16px at ≤600px width, and no rule out-specifies the catch-all (R62, Phase 15)**: the invariant is absolute — every `input`, `select`, and `textarea` on every hub, including ones added after this line was written, must render at exactly 16px on a touch-width viewport, with no exceptions. Two checks: (1) `grep -nE 'font-size:[^;]*!important' index.html` must return **exactly one line** — the catch-all itself (`input, select, textarea { font-size:16px !important; }`, inside the `@media(max-width:600px)` block). A second `!important` font-size is the *only* thing that can defeat the catch-all, so one line means the invariant holds for every rule in the file, including ones written after this. **Do not "improve" this into a grep that enumerates sub-16px rules** — the obvious version (`font-size:1[0-5]px` on an input-ish selector) is line-based and therefore blind to every multi-line rule, which in this file includes `.qf input, .qf select` at 15px, i.e. the exact rule R62 was filed for. A check that cannot see the original defect is worse than no check, because it reports clean. (2) On a real iOS Safari load, or device emulation, at 375px: focus a Compare Cars shortlist price field, the `q-state` selector, a Grow SIP input (any of `sp-*`/`su-*`/`ls-*`), and a loan-panel highlighted input (`l-*` with `.hl`) — none may zoom the viewport.
58. **Dhanam Grow's pre-tax caveat is visible without interaction, on every tab (R63, Phase 15)**: entering `hub-sip` shows the `.sip-caveat` immediately below the section title and above the tab row, with no card to expand and no tab to switch first — true on Monthly, Step-up, and Lumpsum alike, since the caveat sits above all three rather than inside any one tab's content, and true at both 375px and desktop widths. The caveat names the ₹1.25L LTCG exemption, the 12.5% rate, and the 23 Jul 2024 effective date. No corpus, milestone-table, or chart figure anywhere in `hub-sip` is labeled or computed as post-tax — the hub's numbers are unchanged from before Phase 15; only the copy is new.
59. **Reverse Worth bridge shows a real balance sheet's effect, agrees with Dhanam Worth's own projection, and never fabricates a ₹0 (R65, Phase 16)**: with no balance sheet ever saved (fresh `localStorage`, or after "Erase my data"), opening `hub-apartment` → Loan Analysis → "Advanced: Extra Payments Projection" → "What this does to your net worth" (closed by default — expand it) shows the empty-state message and a working link to Dhanam Worth, **never** a ₹0 or blank figure. After saving any non-zero balance-sheet field in Dhanam Worth, reopening the same card (from a *different* session/reload, without visiting `hub-worth` again first) shows real figures — this is the check that `worthSnapshot()` reads `DS.worth`, not the `w-a-*`/`w-l-*` DOM inputs, since those don't exist in the DOM until `hub-worth` is opened this session. The card's "Your Worth projection today" line must equal, to the rupee, what Dhanam Worth's own "Projected Net Worth" card shows at +10 Years for the same saved data (open both and compare — they call the same `calcNetWorthProjection()`, so they cannot legitimately differ). The "With this plan's interest saved" line equals the baseline plus the exact interest-saved figure already shown in the hero above (`#adv-hero-value`) — changing `adv-extra-emi`/`adv-extra-lump` moves both the hero and this card's second line together, live. Entering a "Home Loan Outstanding" balance in Dhanam Worth makes it appear by name in the card's opening fact line. The card is closed by default on every fresh load (decision: lighter than extending `.amounts-hidden`, since that mechanism doesn't reach outside `#worth-main` anyway) and "Hide amounts" in Dhanam Worth has no effect on it — expected, not a bug, per the recorded decision. Verdict-cards-as-personal-advice (`UX-ANALYSIS.md` §Strategic-2's second promise) was deliberately **not** built — nowhere in this card does language read as a recommendation ("you should," "switch to this plan"). **The card's two figures measure different horizons, and both gaps must stay stated on screen (added in Phase 16's code review):** the second line adds the plan's *whole-loan* interest saving to a +10-year projection even though that saving accrues through the payoff year, and the extra payments' own cash cost is counted nowhere — so the labels must name the payoff year ("through yr N") and the caveat must name both the horizon gap and the ₹X/yr the plan costs, ending by calling the second line an upper bound rather than a projection. Changing the horizon, the labels, or the caveat without preserving both disclosures re-introduces an overstated headline: at ₹50L/8.75%/20yr with one extra EMI a year, the saving is ₹10.92L accruing to year 16.7 while the plan has consumed ₹4.42L of cash by year 10.
60. **Error panel surfaces failures, never sends anything, and doesn't storm (R66, Phase 16)**: with devtools open, run `window.onerror('test', 'test.js', 1, 1, new Error('manual test'))` (or throw inside any `on*` handler via the console) — a dismissible panel appears near the bottom of the screen naming the current `BUILD_STAMP`, the error message, and source/line/column, with a working "Copy diagnostics" button (paste afterwards to confirm the clipboard actually received the text) and a "✕" that dismisses it. Throwing the *same* error again updates the existing panel with a `(×2)`, `(×3)`… counter instead of stacking a second panel; throwing a *different* error replaces it and resets the counter — confirm by triggering two distinct messages back to back. Reject a promise with no `.catch` (e.g. `Promise.reject('manual test')` in the console) — the same panel appears via `unhandledrejection`, proving `window.onerror` alone wouldn't have caught it. **With the Network tab open and recording, trigger several of the above** — confirm zero requests fire (no fetch/XHR/beacon of any kind); this is the check that keeps the About page's "you'll see no request leave this page" claim true. Reload the page after dismissing the panel — it does not reappear (no error state is persisted, by design). This environment has no browser, so this item was written from code inspection only and has not been run against a live page — flag it if the on-screen behavior doesn't match.
61. **Keyboard & ARIA pass is complete and live (R8, Phase 17)**: `grep -c '<div class="collapse-header"\|<div class="adv-header"\|<div class="sip-header"' index.html` returns 0. Tab through the whole app with no mouse: every collapse card, the hub nav, the SIP planner tabs, the car mode selector, and every button/input/select receives a visible gold focus ring. Opening/closing any collapse card (including the Worth-bridge card, `#adv-worth-card`) toggles that button's `aria-expanded` between `true`/`false` — check in devtools' Accessibility/Elements panel, not just visually. Switching hubs or SIP planner tabs moves `aria-selected="true"` to exactly one tab in each tablist. Switching the car financing mode moves `aria-pressed="true"` to exactly one of the three mode buttons. `#l-remember`, `#w-hide-btn`, and `#w-import-file` all announce a real accessible name via a screen reader or devtools' accessibility tree, not just visible adjacent text. This environment has no browser, so this item — like item 60 — was written and verified from code inspection only; flag it if live behavior doesn't match.
62. **First-run orientation line dismisses permanently and survives erase (R25, Phase 17)**: on a fresh `localStorage`, `#landing-orientation` appears once above the tile grid; clicking its "✕" hides it and it does not return on reload. Devtools → Local Storage: a `dhanam.orientationSeen` key appears, separate from `dhanam.v1`/`dhanam.seen`. "Erase my data" (in Dhanam Worth) does **not** resurrect the line — `dhanam.orientationSeen` is untouched by `eraseState()`, by design. A restored JSON backup does not carry the dismissal state onto a new device (the key isn't part of the backup blob), so a fresh browser profile importing a backup still sees the line once.
63. **Inline term definitions are reachable, honest, and don't overflow (R23, Phase 18)**: tap each of the 17 "ⓘ" glyphs (`grep -c 'class="term-info-btn"' index.html` should return 17) — perquisite value and the tax-regime label in `hub-car`'s lease panel; pre-EMI and tranche in the loan-disbursement panel; step-up, and CAGR on the Buy-vs-SIP card, Grow's Monthly tab, and Dhanam Worth's projection card; corpus on Grow's Monthly tab; floor rise, amenities, corpus fund, stamp duty, registration, and GST on construction in the Detail panel; IRDAI depreciation in Compare Cars' absorbed loan-detail view; investable-assets/flat-property on Dhanam Worth's projection hero. Each definition appears in under a second, is keyboard-reachable (Tab to the glyph, Enter/Space opens it), and Escape closes it and returns focus to the glyph. Opening a second one closes whichever was already open — never two on screen at once. At 375px width, no popover causes horizontal page scroll, and one placed near the bottom of a tall `.panel-card`/`.collapse-card` (e.g. GST on Construction, near the bottom of the Detail panel's Government Charges card) is **not** clipped by that ancestor's `overflow:hidden` — confirming the `position:fixed` choice actually works, not just the `position:absolute` version that would have looked fine everywhere except that one case. Scrolling the page while a popover is open closes it rather than leaving it detached from its trigger. This environment has no browser, so — like items 60–62 — this item was written from code inspection only and has not been run against a live page.

### Styling

All CSS is inline in `index.html`, using custom properties defined on `:root` — a "Quiet Luxury / Private Bank" theme: a true-neutral near-black base (`--bg`, `--surface`, `--surface2`, `--surface3`, `--border`) with gold as the sole hero accent (`--accent`, `--accent2`) and disciplined green/red status colors (`--green`, `--red`) for real financial deltas. Fonts are `Inter` (body), `Playfair Display` (headings/large numbers), and `DM Mono` (numeric values), **self-hosted** (Phase 4, R5) as `woff2` files under `fonts/` rather than loaded from Google Fonts — the app's privacy claim ("nothing is ever sent anywhere") requires no outbound font request, and self-hosting also makes fonts precacheable for offline use. Each family ships **two** `@font-face` rules per weight-range — one for `unicode-range: U+0000-00FF...` ("latin") and one for `unicode-range: U+0100-02BA...20AD-20C0...` ("latin-ext") — because the plain "latin" subset Google normally serves **excludes `₹` (U+20B9)**, which this app prints constantly. Any future font change must keep both ranges; dropping the latin-ext file would silently replace every ₹ with a fallback-font glyph. Reuse existing custom properties and utility classes (`.panel-card`, `.collapse-card`, `.section-title`, etc.) rather than introducing new color values or one-off components.

**Touch-width input sizing is a catch-all rule, not a selector list (D7, hardened R62/Phase 15).** The `@media(max-width:600px)` block near the top of the `<style>` forces `font-size:16px !important` on every `input`, `select`, and `textarea`, because **mobile Safari auto-zooms any focused input under 16px** and the page lurches on every field tap. This used to be an explicit list of class selectors, and the list itself was the defect: it failed **six** times in **three** distinct modes. (1) *Never added* — `.cc-field input, .cc-field select` shipped at 14px in Phase 9 and grew in Phase 14; `.qf select` shipped at 15px in Phase 6a. (2) *Listed but out-specified* — `.field.highlight input` and `.loan-field input.hl`, both (0,2,1), out-ranked the list's own `.field input`/`.loan-field input` rules at (0,1,1) and still rendered 15px on mobile. (3) *Listed but out-ordered* — `.sip-inline-input` and `.disb-tr-field input` are **declared below the media block** at equal specificity, so plain source order beat the list and they too still rendered 14px. **Mode (3) is the one that matters most for anyone tempted to go back to a list**: it needs no new class and no specificity mistake — merely declaring a rule further down the file silently defeats an entry that is sitting right there in the block, which no amount of "remember to add your class" diligence would ever catch. A comment asking the next author to remember had, by then, already failed six times; R62 replaced the mechanism rather than extending the list a seventh. The catch-all is safe because no `input`/`select`/`textarea` rule anywhere in `index.html` sets a font-size above 16px (15px is the current maximum), so `!important` here can only enlarge a rule, never shrink one that was deliberately made small. **Any future input/select/textarea rule is covered automatically — there is nothing to add here anymore.** See manual checklist item 57 for the standing verification.

**Shipped: Dhanam Grow states its pre-tax assumption (R63/Phase 15).** One `.sip-caveat` sits in `hub-sip`, immediately after the `.section-title` and above `.sip-planner-tabs` — **once, above the tab row, not once per tab.** Decision made and recorded here: the D4 density argument (state it once, not three redundant times) won over the "a reader who opens only one tab should still see it" argument, because placing it above the tab row means every tab-open path already passes it — there's no tab a visitor can land on without scrolling past this div first, so the per-tab argument's premise (a reader might see only one tab and never this caveat) doesn't actually hold here the way it does for `hub-apartment`'s Buy-vs-SIP caveat (which sits *inside* one specific collapsible panel, not above a tab row every path already crosses). Copy-only, per Phase 7b's `state-and-don't-model` assessment (`TASK-UX-REDESIGN.md`) — states the ₹1.25L LTCG exemption and 12.5% rate (Union Budget 2024, effective 23 Jul 2024) and that actual tax depends on redemption timing the app can't know. **Computes nothing** — no `calc.js` change, no post-tax figure anywhere in the hub; that remains R31, still gated on the unanswered B10. See manual checklist item 58.

### Icons (R7, Phase 4)

UI-chrome icons (nav tabs, landing tiles, action-row buttons, panel-card headers, section titles, collapse headers, buttons) are inline SVGs, not emoji — emoji render inconsistently across platforms/fonts, which is why R7 replaced them. One hidden sprite of `<symbol id="i-name">` elements sits in a `<svg style="position:absolute;width:0;height:0">` right after `<body>`; every usage is `<svg class="icon" aria-hidden="true" focusable="false"><use href="#i-name"></use></svg>` — the `aria-hidden`/`focusable` pair (R27, Phase 4b) keeps every icon out of the accessibility tree and the tab order, since the adjacent text label is always the real accessible name. `.icon` is sized in `em` (scales with the wrapping element's font-size, matching how the emoji it replaced used to scale) and colored via `currentColor` only — never a fixed hue, so palette rule 1 (no new hues) isn't at risk just because an icon sits inside a gold-colored active state. **`.tile-icon`/`.ab-icon` are a deliberate exception**, pinned to `var(--text-mid)` instead of inheriting (R28, Phase 4b): `.tile-title` is permanently gold as a design choice rather than a state, so an inheriting tile icon would be gold on every tile at all times, competing with the headline; `.action-btn.active`'s gold is a real state change and the pin keeps that gold reserved for the text instead of doubling it onto the icon too. **Emoji intentionally remain in body/warning copy** (e.g. the disbursement tranche-sum warning) — that's out of the icon set's scope. When adding a new icon-slot usage, check the existing `<symbol>` set for a semantic match before drawing a new one — and confirm it's actually referenced somewhere, since an unused `<symbol>` is dead weight (R30 deleted one); icons here are decorative reinforcement of an adjacent text label, not the sole carrier of meaning, so reuse across related concepts (e.g. one house icon for both `⌂` and `🏠`, one banknote icon for all money-adjacent emoji) is preferred over a 1:1 emoji-to-icon mapping. One exception to the emoji→`<use>` swap pattern: anywhere an icon's state toggles at runtime (e.g. the Worth hub's hide-amounts eye/eye-off), the element can't be updated via the `set(id, txt)` helper (`textContent` can't hold an `<svg>`) — split into a static `<use>` plus a separately-`id`'d label span, and swap the `<use>` element's `href` attribute directly instead.

**Color rules (see `COLOR-PALETTE-ANALYSIS.md` for the full rationale):**
1. Exactly three chromatic hues exist in the app: gold, green, red. Everything else is neutral (the true-black scale + warm-cream text). No fourth hue gets added without revisiting that document first.
2. Gold is the one hero/emphasis color — the primary highlighted answer in any card, active/selected states, focus rings. Not used for section identity or decoration.
3. Green is a real, positive financial delta only — never "this is a big/important number" alone, and never UI chrome unless the action itself represents a gain.
4. Red is a real cost or negative financial delta only — same rule as green, mirrored.
5. Color is never the only signal — colored values keep an adjacent text label; comparison verdicts keep their emoji/text framing.
